# SDD：講經影片 → 向量知識庫 Pipeline

> 版本 0.3 ｜ 目標讀者：實作者（人類或 AI coding agent）
> **本文件第 5 章「驗證」在第 6 章「實作」之前。任何實作動作開始前必須先讀完第 5 章。**

> **v0.2 修訂（2026-08-04）**——依真實素材 `zIglvjoU9vo` 的實測結果修訂。
> 觸發原因：v0.1 的 §1.3 把單支影片的觀察當作已確認事實，其中兩項與素材
> 不符，錯誤傳導到 §4.3 的分類設計，最終產出的 chunk 是攝影棚背板的裝飾字
> ——而 §5.3 的十條不變量與 §5.4 的溯源檢查**全部通過**。
>
> | 章節 | 變更 |
> |---|---|
> | §1.3 | 素材特性表**降級為單支影片的 profile 範例**，不再是設計前提；更新畫面切換與純講者佔比；新增鏡頭運動一列 |
> | §2.2、§4.0 | **新增 S-1 素材勘查階段**，新系列開跑前強制執行，不符即中止 |
> | §4.3 步驟 2 | speaker/slide 分類由**人臉大小**改為**背景距離 + Otsu**（v0.3 再整個移除，改由 VLM 判定） |
> | §4.3 步驟 5 | keyframe 由「取段末幀」改為「取段內 ink 量最大的幀，排除兩端」 |
> | §5.1(A) | 新增對抗樣本 **A8（鏡頭運動）、A9（交叉淡化）** |
> | §5.1(B) | 補上**黃金集標註規範** |
> | §9 | 更新風險表，移除三項已解除的 |
>
> 實測依據：`experiments/r8_overlay/REPORT.md`、`docs/decisions.md` D7–D14。

> **v0.3 修訂（2026-08-04）**——**移除本地 OCR 鏈，改由 VLM 分類與理解。**
>
> 觸發原因：R8 證明攝影棚幀（81%）不含投影片內容，因此不需要精確的
> speaker/slide 二分類；而本地 OCR 鏈（PaddleOCR + 詞庫 + 拼音校正）累積了
> 一串與本專案無關的坑（oneDNN 相容性、paddle 版本、簡繁字集、jieba 詞典
> 是簡體）。
>
> | 章節 | 變更 |
> |---|---|
> | §2.2 | 資料流移除 S2／S2b／S2c，十階段縮為七階段 |
> | §4.3 步驟 2 | speaker/slide 分類**整個移除**——CV 只找靜止區段，分類交給 S4 |
> | §4.3 步驟 5 | 代表幀取「段內 ink 量最大、排除兩端」 |
> | §4.4、§4.5 | **刪除**（S2 OCR、S2b 詞庫、S2c 術語校正） |
> | §4.6 | S3 只做粗切——語意吸附需要投影片文字，v0.3 拿不到 |
> | §4.7 | S4 新增兩項職責：`is_slide` 判定、術語校正 |
> | §5.4 | 溯源比對來源由本地 OCR 改為 VLM 讀出的文字（**保護強度下降**） |
> | §9 | 新增三項風險 R9／R10／R11 |
>
> **最重要的代價**：§5.4 的溯源檢查失去了獨立的比對基準。原本來源是本地
> OCR（與 LLM 是兩個系統），現在來源是 VLM 自己讀出的文字。模型若系統性
> 誤讀，兩者會一致地錯，檢查全數通過。詳見 `docs/known-risks.md` R9。
>
> 實測依據：`docs/decisions.md` D16、`experiments/vlm_pipeline/`。

> **v0.4 修訂（2026-08-06）**——**S-1 改為逐支跑，S4 拆為 S4a/S4b/S4c。**
>
> 觸發原因兩項，都是實測：
>
> 1. **同一個播放清單的四集，畫面結構就不一致。** 第 14、27 集換了攝影棚
>    背景（木質牆 vs 水墨山景），CV 前篩剔除率從 28/49 到 4/21 都有。
>    「每個系列跑一次 S-1」的假設不成立——把第 1 集的 profile 套到第 14 集，
>    與 v0.1 把單支影片套到整個系列是同一個錯誤的不同尺度。
> 2. **S4 一次做四件事，讓三個問題無法分開處理**：同一張投影片被重複轉錄
>    10 次（基準在抖，變異 5.2%）、不同子工作的最佳模型不同、
>    批次多模態呼叫造成 **30.6% 的區段拿到隔壁的圖**。
>
> | 章節 | 變更 |
> |---|---|
> | §2.2 | 資料流：S-1 移到 S0 之後且逐支跑；S4 拆為 S4a/S4b/S4c |
> | §2.3 | 模型分工改為**逐子階段可設定**，附四模型對打實測 |
> | §4.0 | S-1 **逐支跑**；輸出加逐支 profile；新增「背景基準變動」判準 |
> | §4.7 | 拆為 §4.7a/4.7b/4.7c；新增多模態綁定規則（D20） |
> | §4.7(b) | **校正的授權條件改寫**——原本的「正確寫法出現在投影片上」
>   實測鑑別力為負；改為「發音沒變、只是字寫錯」＋三道程式端閘（D19） |
> | §4.2 | ASR 輸出統一轉繁（`s2tw`），只轉 ASR 不轉手動字幕（D24） |
>
> **未納入的**：投影片去重（S1c）仍是提案
> （`docs/proposals/v0.4-transcript-first.md`），本次不納入規格。
> S4a 在它落地前退化為逐候選幀處理——行為正確，但仍會重複轉錄。
>
> 實測依據：`experiments/r21_bakeoff/`、`experiments/r14_local_vlm/`、
> `docs/decisions.md` D19–D25。

---

## 1. 目標與非目標

### 1.1 目標

把 YouTube 上的中文講經／授課影片系列，批次轉換成**可檢索、可溯源的結構化知識庫**，輸出為向量庫可直接匯入的 JSONL。

具體成功條件：

1. 給一個 YouTube playlist URL，能無人值守地跑完整個系列（受每日 API 額度限制，跨日續跑）。
2. 輸出的每一個 chunk 都自足（不依賴前後文即可理解）、帶時間戳、可回溯到影片的具體秒數。
3. 投影片上的內容以**文字**形式進入知識庫（因為 RAG 讀不到圖），且保留版面所隱含的結構關係。
4. 逐字稿中的專有術語被投影片文字校正過。

### 1.2 非目標（明確排除）

- **不做 UI**。CLI + 設定檔。
- **不做 Obsidian 整合**。輸出 JSONL，使用者自行後處理。
- **不做即時／串流處理**。批次離線。
- **不追求 SOTA 偵測精度**。這是個人知識庫工具，不是論文。錯了可以重跑。
- **不做多模態向量檢索**（CLIP embedding）。v1 只做文字檢索。
- **不做跨影片概念圖 / concept registry**。推遲到 v2（見 §7.5）。

### 1.3 目標素材的特性

> **這張表是 `zIglvjoU9vo` 單支影片的實測 profile，不是系列通則。**
>
> 本文件 v0.1 曾把它當作「已確認事實」並據以設計 §4.3。實測發現其中兩項
> 描述與影片不符，而錯誤一路傳導到產品：chunk 內容變成攝影棚背板的裝飾字
> 「里明品带务务念」，但管線的每一項機械檢查都是綠的。
>
> 為避免同類錯誤，**每一支影片開跑前都必須先跑 §4.0 的 S-1 素材勘查**
> （v0.4 由「每系列一次」改為逐支），由它產生該支自己的 profile。
> 下表因此改為「S-1 對**第 1 集**的輸出範例」，不再是設計前提。
> 凡是與本表不符的素材，S-1 會在第一天就報出來。
>
> **v0.4 補充**：實測同一個播放清單的第 14、27 集**換了攝影棚背景**，
> 下表的畫面切換比例對那兩集不成立。系列內部就不齊，
> 這也是 S-1 必須逐支跑的直接證據。

`zIglvjoU9vo`（42:00，2519 幀 @1fps，游朱義《古典醫學之人體設計系列-1》）
的 S-1 實測結果：

| 特性 | 實測狀態 | 對設計的影響 |
|---|---|---|
| 畫面切換 | **固定攝影棚機位（講者始終在畫面中，佔 81.1%）** ↔ 全螢幕投影片（18.5%）。切換**多為硬切**（72 個邊界中 62 個），**少數邊界有 1 秒交叉淡化**（10 個，佔全片 0.40%） | 不需要 ROI 裁切；v0.3 起**不做 CV 分類**，「是不是投影片」由 S4 的 VLM 判定（§4.3 步驟 2）；keyframe 選取須避開轉場幀（§4.3 步驟 5） |
| 字幕 | **播放器渲染**（CC 可關閉），本片有 zh-Hant **手動**字幕 | yt-dlp 取得乾淨影片 + 免費字幕軌；**不需要遮罩模組**。手動字幕品質遠高於 ASR，§4.2 優先採用 |
| 雷射筆 | 有，小紅點會移動 | 降解析度 + 高斯模糊即可壓制 |
| 鏡頭運動 | **有攝影機推近**（例如 t=199→201、t=554→556） | 任何以「畫面變化」為基礎的偵測都會受影響；§5.1 的 A8 為此而設 |
| 投影片內容 | 密集中文、含**文言文**、含**直排文字**、版面帶語意（箭頭、雙欄、色彩編碼） | Tesseract 不可用；VLM 必須輸出結構化版面描述 |
| 純講者時段 | **81.1%**（2043/2519 幀） | IR 必須能表達 `mode: speaker_only` |
| 內容層次 | 經文原文 / 白話解說 / 口頭延伸 三種並存 | chunk 需帶 `content_type` 供分別檢索 |

**明確不存在的模式**：v0.1 的修訂過程中一度以為存在「投影片內容半透明疊加
在講者畫面上」的第三種模式。逐秒檢視 2519 幀後確認，那是**交叉淡化轉場幀**
（全片 10 幀），不是持續的呈現模式。**因此 IR 不需要第三種 mode。**
量測見 `experiments/r8_overlay/REPORT.md`。

---

## 2. 系統概觀

### 2.1 核心設計原則

**原則一：拆時間軸，不拆模態軸。**
逐字稿與投影片的「提取」可以分開平行；但「理解」必須是聯合的。分別理解兩個模態再合併，會在壓縮階段丟掉對齊所需的資訊，且不可逆。降低單次工作量的方法是**縮小時間範圍**（一次一個 segment），不是拆開模態。

**原則二：本地扛量，雲端扛判斷。**
所有高頻、機械性的工作（ASR、CV）在本地執行，不受額度限制。只有需要跨模態判斷力的那一步呼叫 Gemini API。

**v0.3 調整了這條線的位置**：原本 OCR 與 embedding 也算「機械性工作」而放在
本地，實測發現本地 OCR 帶來的維護成本（見 §4.4／4.5）高於它省下的額度——
一支 42 分鐘影片只要 18 次請求。判斷力與機械性的界線因此往本地這側收，
CV 只留下「找出畫面不再變化的區間」這件真正機械的事。

**原則三：不花額度的工作先跑滿，花額度的工作按日消化。**
S0–S3 是 producer，可以先把整個 playlist 處理完囤在硬碟；S4 是 consumer，每天按額度消化 buffer。額度重置時工作已就緒，一秒不浪費在 ffmpeg 上。

**原則四：每個階段落地成檔案，可獨立重跑。**
批次跑數十支影片時，中途失敗是常態而非例外。

### 2.2 資料流

```
S0  取得        yt-dlp → 影片 + 字幕軌 + metadata
                 │
                 ▼
S-1 素材勘查    **每支都跑**（v0.4；原為每系列抽樣一次，見 §4.0）
                 → profile：畫面模式分布、停留時間、轉場型態、鏡頭運動
                 → 與本系列既有 profile 不符時**中止並報告**
                 → 與 S1b 共用抽幀，邊際成本接近零
                 │
      ┌──────────┴──────────┐         ← 平行，皆為本地、無額度限制
      ▼                     ▼
S1a 逐字稿              S1b 靜止區段偵測
    YouTube 字幕優先        抽幀 → ink Jaccard + HMM
    Whisper 補強/驗證       → 每段取內容最完整的代表幀
    ASR 輸出轉繁（D24）     │
      │                     │
      └──────────┬──────────┘
                 ▼
S3  對齊              以候選區段粗切 → 指派逐字稿 → segments
                 │
                 ▼
S4a 投影片理解  ★     **逐張候選幀一次**（v0.4 拆分，見 §4.7）
                       ├ is_slide：這張圖是不是投影片？
                       ├ slide_text：逐字轉錄（§5.4 的溯源來源）
                       └ description：版面結構與語意
                 │
                 ▼
S4b 詞庫校正          **純本地、不花額度**
                       以 slide_text 建詞庫 → 掃逐字稿的同音異寫
                       → 提案仍走 §4.7b 的三道閘
                 │
                 ▼
S4c 逐段理解  ★★      per segment：逐字稿 + 該時段的 slide_text/description
                       ├ corrections：對照投影片修正逐字稿錯字
                       └ content_blocks：結構化理解
                 │
                 ▼
S5  全片統整          只讀 S4c 輸出，不再看圖
                 │
                 ▼
S6  渲染              chunks.jsonl（產品）+ debug markdown（人工抽檢用）
```

★ 標記為需要模型的階段。**S4a 與 S4c 可用不同模型**，見 §2.3。

### 2.3 模型分工

| 用途 | 階段 | 模型 | 位置 |
|---|---|---|---|
| ASR | S1a | faster-whisper large-v3 | 本地 GPU（有手動字幕時不跑，§4.2） |
| 靜止區段偵測 | S1b | ink Jaccard + 2 狀態 HMM | 本地 CPU |
| **是否為投影片** | S4a-1 | **可設定**（見下） | — |
| **投影片逐字轉錄** | S4a-2 | **可設定**（見下） | — |
| **版面描述** | S4a-3 | **可設定**（見下） | — |
| 詞庫事後校正 | S4b | **無模型**，純程式 | 本地 CPU |
| **逐段理解** | S4c | **可設定**（見下） | — |
| 全片統整 | S5 | 可設定 | — |

**v0.4：模型改為逐子階段可設定，不再綁定單一模型。**

理由是實測（`experiments/r21_bakeoff/`）：**同一批素材上，不同子工作的
最佳模型不同**，差距大到不能忽略。

| | is_slide | slide_text CER（跨集平均） | 中位耗時 |
|---|---|---|---|
| gemma4:12b | **90.5%** | 37.8%※ | 20.5s |
| qwen2.5vl:7b | 66.7% | **4.9%** | **1.7s** |
| gemma3:12b | 71.4% | 7.9% | 2.9s |

※ gemma4 在 16 GB 上無法可靠轉錄長文投影片——`num_ctx=8192` 時請求
不會完成，開到 16384 後 KV cache 被擠到 CPU、單張 225 秒且仍不穩。
**這是硬體限制不是能力差異**，換卡後結論可能不同。

**這個設計順帶補回 R9 的獨立性缺口**：S4a 讀出 `slide_text`、S4c 產生
`content_blocks`，兩者若為不同模型，§5.4 的溯源檢查就變成「拿一個模型的
產出去驗另一個模型讀出的來源」。v0.3 把兩件事交給同一個模型，
是 §9 R9 記的主要代價。

**VRAM 約束下的執行順序**：16 GB 放不下兩個 VLM 同時常駐。
S4a 與 S4c 是**先後**的階段，所以按階段序列化載入即可，
不需要同時在記憶體中。這是把約束交給流程設計而非模型選擇。

**額度來源明確為 Gemini API key（AI Studio 取得，free tier）。訂閱額度
（AI Pro/Ultra）在架構上無法接入程式化呼叫，不得嘗試繞道。**
子階段配置為本地模型時，該階段不消耗額度；§6 的額度估算須依實際配置計算。

**v0.3 移除的兩項**：本地 OCR（PaddleOCR-VL）與 embedding（BGE-M3）。
前者的職責併入 S4，後者隨 §4.6 的語意吸附一起移除。
**v0.4 讓詞庫以另一種形式回來**（S4b），但來源是 `slide_text` 而非本地 OCR，
且只提案不直接改寫——見 §4.7b 與 `docs/decisions.md` D25。

VRAM 規劃（RTX 5070 Ti，16GB）：任兩個大模型**不可同時常駐**，必須分階段
載入／卸載。這在分階段設計下是自然的。

---

## 3. 中介表示（IR）Schema

IR 是整個系統的契約。所有階段圍繞它。

### 3.1 檔案佈局

```
work/
  {video_id}/
    00_meta.json           S0
    01_video.mp4           S0
    01_captions.vtt        S0（若有）
    02_frames/             S1b 抽出的縮圖（可清理）
    02_candidates.json     S1b
    03_slides/             S1b 選定的候選幀（保留）
      slide_001.png ...    是不是投影片由 S4a 判定（is_slide）
    05_transcript.json     S1a（S4b／S4c 的術語校正會就地更新 text_corrected）
    06_segments.json       S3
    07_understanding/      S4c（每 segment 一檔，便於斷點續跑）
      seg_001.json ...
    08_video.json          最終 IR
    state.json             階段完成狀態與參數 hash
out/
  profile/{series_id}.json S-1 素材勘查
  chunks.jsonl             S6 產品
  debug/{video_id}.md      S6 人工抽檢用
  quota.db                 額度帳本
```

### 3.2 Slide 物件

```json
{
  "slide_id": "slide_005",
  "image_path": "03_slides/slide_005.png",
  "t_first_seen": 748.3,
  "t_last_seen": 845.1,
  "is_progressive_final": true,
  "build_frames": [750.0, 762.4, 771.9],
  "ocr_text": "太上老君內觀經\n天地媾精，陰陽布化...",
  "ocr_confidence": 0.87,
  "layout_description": null
}
```

- `is_progressive_final`：此圖是否為逐條動畫段落的最後一幀（內容最完整）。
- `build_frames`：該段落中被合併掉的中間幀時間戳，保留供 debug。
- `layout_description`：由 **S4a** 填入的結構化版面描述。

### 3.3 Segment 物件

```json
{
  "segment_id": "zIglvjoU9vo#007",
  "video_id": "zIglvjoU9vo",
  "t_start": 748.3,
  "t_end": 845.1,
  "mode": "slide",
  "slide_ref": "slide_005",
  "transcript_raw": "...時運開始進入...",
  "transcript_corrected": "...識蘊開始進入...",
  "corrections": [
    {"from": "時運", "to": "識蘊", "source": "slide_005", "method": "lexicon", "score": 0.91}
  ],
  "boundary_method": "semantic_snap",
  "boundary_shift_sec": -6.2,
  "understanding": { ... }
}
```

- `mode`：`"slide"` ｜ `"speaker_only"` ｜ `"transcript_only"`（降級模式，見 §4.7）
- `boundary_shift_sec`：語意吸附相對於純時間戳切點的位移，供驗證用。

### 3.4 Understanding 物件（S4c 產出；`slide_text`／`layout_description` 由 S4a 填入 Slide）

```json
{
  "summary": "本段解說《一月為胞，精血凝也》...",
  "layout_description": "上方為四張胚胎顯微照片（第一至第四周），下方紫底區塊為經文引文與白話解說。",
  "content_blocks": [
    {
      "type": "經文原文",
      "text": "一月為胞，精血凝也。",
      "provenance": {"kind": "slide_ocr", "ref": "slide_005"}
    },
    {
      "type": "白話解說",
      "text": "父母陰陽和合後，受精卵一個月生長成為「胞」，是父精母血的凝聚物。",
      "provenance": {"kind": "slide_ocr", "ref": "slide_005"}
    },
    {
      "type": "口頭延伸",
      "text": "講者以簽約作比喻說明識蘊進入的時機。",
      "provenance": {"kind": "transcript", "ref": "748.3-845.1"}
    }
  ],
  "terms": ["識蘊", "胞", "精血"],
  "unverified_claims": []
}
```

**`content_blocks[].type` 的封閉列舉**：`經文原文` / `白話解說` / `圖表描述` / `口頭延伸`。
**`provenance` 為必填欄位，不得為 null。** 見 §5.4。

### 3.5 輸出 Chunk（S6，JSONL 每行）

```json
{
  "id": "zIglvjoU9vo#007#b02",
  "text": "父母陰陽和合後，受精卵一個月生長成為「胞」...",
  "metadata": {
    "video_id": "zIglvjoU9vo",
    "series_id": "PL37L2Y8jeuGDeSRi5ntD_gK1pBM8KlSkv",
    "video_title": "...",
    "episode_index": 3,
    "t_start": 748.3,
    "t_end": 845.1,
    "url": "https://www.youtube.com/watch?v=zIglvjoU9vo&t=748s",
    "content_type": "白話解說",
    "slide_ref": "slide_005",
    "terms": ["識蘊", "胞"],
    "provenance_kind": "slide_ocr"
  }
}
```

**Chunk 自足性要求**：`text` 欄位不得包含「如上圖」「前面提到的」等指涉性語句，也不得包含 markdown 裝飾。若原始表述有指涉，S4c 必須在生成時展開。

---

## 4. 各階段介面契約

每個階段定義：輸入、輸出、冪等鍵、失敗行為。

### 4.0 S-1 — 素材勘查

**每支影片都跑，不是每個系列跑一次。**

> **v0.4 修訂。** 原文寫「每個新系列開跑前執行一次，不是每支影片都跑」，
> 那是錯的。2026-08-06 實測同一個播放清單的四集（`experiments/r21_bakeoff/`
> §6）發現**系列內部的畫面結構就不一致**：
>
> | | 第 1 集 | 第 5 集 | 第 14 集 | 第 27 集 |
> |---|---|---|---|---|
> | 攝影棚背景 | 水墨山景 | 水墨山景 | **木質「真普宗」牆** | **木質牆** |
> | CV 前篩剔除率 | 28/49 | 9/30 | 11/37 | 4/21 |
>
> 背景換了，「與全片中位幀的距離」這個機制的分界值也就跟著換。
> 抽樣 2–3 支得到的 profile **推廣不到同系列的其他集**。
>
> 成本上這也不划算——S-1 的抽幀與 S1b 完全重疊，逐支跑實際上不增加工作，
> 只是把既有的量測結果留下來。

**存在的理由**：v0.1 的 §1.3 把單支影片的觀察當作系列通則，其中兩項描述
與實際素材不符（畫面切換型態、講者佔比）。錯誤一路傳導到 S1b 的分類設計，
最終產品的 chunk 內容是攝影棚背板的裝飾字——而 §5.3 的十條不變量、§5.4 的
溯源檢查**全部通過**。機械檢查抓不到「素材與假設不符」這類錯誤，因為每一
步都忠實執行了錯誤的假設。S-1 的職責就是在第一天把這件事抓出來。

**v0.4 補充**：這個理由對「系列內部」同樣成立。把第 1 集的 profile 套到
第 14 集，與 v0.1 把單支影片套到整個系列，是同一個錯誤的不同尺度。

- **輸入**：單支影片（S0 取得後、S1b 之前）
- **輸出**：
  - `out/profile/{video_id}.json`——逐支的量測結果
  - `out/profile/{series_id}.json`——系列彙總，**記錄跨集的離散度**
    （背景是否一致、各項量測的四分位距）。這是「系列有多不齊」的證據，
    不是拿來當通則的
- **成本**：純本地 CV，無額度消耗。**與 S1b 共用抽幀**，逐支跑的邊際成本
  接近零

**必須量測的項目**：

| 項目 | 量測方式 | 用途 |
|---|---|---|
| 畫面模式分布 | 與全片中位幀的距離 + Otsu（同 §4.3 步驟 2） | 確認「講者 vs 投影片」的比例，以及是否真的只有兩種模式 |
| 模式分離度 | 兩群邊界值之比 | 低於 3x 代表二分類不可靠，須人工檢視 |
| 投影片停留時間分布 | 中位數、四分位、最短／最長 | §4.3 的 HMM 先驗（`self_transition`）是否合適 |
| 轉場型態 | 邊界處的混合幀數量與比例 | 硬切 vs 交叉淡化，決定 keyframe 選取策略 |
| 鏡頭運動 | 攝影棚幀內部的畫面位移量 | 有推近／平移時，以畫面變化為基礎的偵測會受影響 |
| 字幕可用性 | yt-dlp 的 subtitles / automatic_captions | 決定 S1a 走手動字幕還是 Whisper |

**失敗行為**：任一項與 §1.3 的 profile 顯著不符時，**中止並報告**，不自動
繼續。「顯著不符」的判準寫在 profile 檢查器中，至少包含：

1. 講者佔比偏離 §1.3 記載值超過 ±20 個百分點
2. 模式分離度低於 3x
3. 投影片停留時間中位數低於 10 秒（暗示分類或偵測失效）
4. 偵測到的畫面模式超過兩種

**v0.4 新增的判準**（逐支跑才驗得到）：

5. **本支的背景基準與同系列前一支的距離超過門檻**時，不中止但**必須記錄**
   ——它代表 §4.3 的分界值要重算，不能沿用。實測第 14 集換了攝影棚背景，
   而前 13 集都沒換

**這個階段不產生知識庫內容**，它只回答「這一支能不能用現有設計處理」。

**與 §1.3 的關係**：§1.3 是**第 1 集**的實測結果，不是系列通則
（v0.2 已把它降級為「profile 範例」）。v0.4 進一步明確：
比對基準應是**本系列已跑過的影片的彙總**，§1.3 只在系列的第一支上當基準。

### 4.1 S0 — 取得

- **輸入**：video_id 或 playlist_id
- **輸出**：`00_meta.json`、`01_video.mp4`、`01_captions.vtt`（若存在）
- **實作**：yt-dlp。字幕優先序：手動字幕 > 自動字幕 > 無
- **冪等鍵**：`video_id`
- **失敗行為**：影片不可用（私人/刪除）→ 記入 skip list，繼續下一支
- **注意**：確認下載的影片**不含硬燒字幕**（已驗證為播放器字幕）

### 4.2 S1a — 逐字稿

- **輸入**：`01_video.mp4`、`01_captions.vtt`
- **輸出**：`05_transcript.json`（含 segment 級時間戳）
- **策略**：
  1. 若有**手動**字幕 → 直接採用，標記 `source: "manual_caption"`
  2. 否則跑 faster-whisper large-v3（`language="zh"`）→ `source: "whisper"`
     （v0.3 移除了 `initial_prompt` 的系列術語詞庫——詞庫來自本地 OCR，
     已一併移除。錯字改由 S4 事後修正，見 §9 的 R11）
  3. 若有**自動**字幕且也跑了 Whisper → 兩者都存，`source: "whisper"` 為主，自動字幕存為 `alt` 供交叉檢查
- **冪等鍵**：`video_id + whisper_model + params_hash`
- **失敗行為**：Whisper OOM → 降低 batch size 重試一次 → 仍失敗則標記影片為 `failed`，繼續下一支

### 4.3 S1b — 投影片候選幀

這是移植 vid2slides 演算法的部分。**移植演算法，不 fork 該 repo**（該 repo 為 2020 年程式碼，conda 環境已不可用）。

- **輸入**：`01_video.mp4`
- **輸出**：`02_candidates.json`、`03_slides/*.png`
- **流程**：
  1. ffmpeg 每 1 秒抽一幀縮圖（可設定）
  2.（v0.3 移除）**不再做 speaker/slide 分類**——見下方「為什麼分類交給 VLM」
  3. 對 slide 幀：降解析度至短邊 ~180px + 高斯模糊（壓制雷射筆紅點與壓縮雜訊）
  4. **HMM 換頁偵測**：以「投影片會停留一段時間」為先驗建模，避免手調門檻
  5. **逐條動畫合併**：若連續段落之間為單調的內容增加，視為同一頁的 build 並合併；合併後**取段內 ink 量最大的幀，且排除段落兩端各 1–2 幀**（見下方「為什麼不取段末幀」）
  6. 每個穩定段落輸出一張 `slide_NNN.png`
- **冪等鍵**：`video_id + fps + detector_params_hash`
- **失敗行為**：偵測到 0 張投影片 → 標記 `mode: transcript_only`，**不中斷**，繼續走 S1a → S3 → S4（無圖模式）

#### 步驟 2：為什麼分類交給 VLM（v0.3）

v0.1 寫「偵測滿版人臉 → 標記為 speaker 幀（本素材為硬切，無 PiP，故此步
足夠）」。實測**不足夠**：

| 量測 | 值 |
|---|---|
| 真實素材的講者人臉面積佔比 | **0.005–0.026** |
| 原設計門檻 `face_min_area_ratio` | 0.04 |
| 合成素材的講者人臉面積佔比 | 0.131（差 5 倍） |
| 全螢幕投影片（胚胎圖）中被偵測到的「人臉」 | **0.024** |

講者是**廣角攝影棚機位**，人臉本來就不滿版；而投影片內含人像時
（醫學素材尤其常見）Haar 會在上面找到人臉，佔比與講者畫面**完全重疊**。
**人臉大小在這份素材上不具區辨力**，調門檻無法解決。

**v0.2 的補救**是改用「與全片中位幀的距離」+ Otsu，實測分離度 5.07x、
對鏡頭運動有 8–10 倍餘裕，可行。

**v0.3 直接移除了這一步。** 理由：R8 證明攝影棚幀（81%）不含投影片內容，
所以不需要精確分類——只需要「找出靜止區段 → 每段取一幀 → 由 VLM 判斷是
不是投影片」。VLM 本來就要看這張圖做理解，多回一個布林值幾乎免費；而 CV
要回答同一個問題需要一整套特徵工程。

**背景距離 + Otsu 的機制保留在 §4.0 的 S-1 素材勘查中**，用途改為「量測
畫面模式分布」而非「逐幀分類」。以下實測數據因此仍然有效：

實測（`zIglvjoU9vo`，2519 幀）：

| 指標 | 值 |
|---|---|
| Otsu 自動門檻 | 0.1665 |
| 判為全螢幕投影片 | 466 幀（**18.5%**） |
| 獨立量測的全螢幕比例 | **18.6%**（吻合） |
| 分離倍數（排除轉場幀） | **5.07x**（攝影棚 max 0.0330 → 全螢幕 min 0.1676） |
| 攝影機推近幀（201、556） | 距門檻 **8.3–10.2x**，正確分類為攝影棚 |

**已知的邊界行為**：10 個交叉淡化轉場幀**全部**落在攝影棚側，因此全螢幕
段落會被系統性少算兩端各 1 幀。這是 ±1 秒的偏差，遠小於 §5.2 的 ±2 秒
容忍窗，**不做修正**。

**量測分離度時必須排除轉場帶。** 交叉淡化幀本質上介於兩類之間，用極值
（攝影棚 max vs 全螢幕 min）量出來是 **1.05x**——量到的是轉場，不是模式的
可分性。用穩健分位數（攝影棚 p99 vs 全螢幕 p1）量出來是 **7.62x**。

S-1（§4.0）的「模式分離度 ≥3x」用的是後者。若用前者，**任何有交叉淡化的
影片都會被誤判為不符**，這條檢查就等於沒有。

#### 步驟 5：為什麼不取段末幀

v0.1 寫「合併並**取最後一幀**」。實測在段落邊界會取到**交叉淡化轉場幀**：
`slide_017.png` 的 keyframe 落在 t=765.5，那正是進入全螢幕投影片
[766, 777] 的轉場幀——畫面是攝影棚與投影片的混合，OCR 讀不出乾淨文字，
VLM 看到的也是模糊疊影。v0.1 修訂過程中一度把這張圖誤判為「半透明疊加
是一種呈現模式」，即源於此。

**改為取段內 ink 量最大的幀，並排除段落兩端各 1–2 幀。**

- 「ink 量最大」保留了逐條動畫取**內容最完整版本**的原意——build 是單調
  增加，最後一個 build 的 ink 量本來就最大，所以對 A2 的行為不變
- 「排除兩端」避開轉場幀。1fps 下排除 1–2 幀即足夠（實測轉場長度皆為 1 秒）
- 段落短於 5 幀時不排除，改取正中間那一幀——否則會無幀可選

#### 步驟 5 的補充：合併判準用視覺包含

v0.1 寫「新段落的 OCR 文字包含舊段落的」。v0.3 已無本地 OCR，且即使有，
S2 在資料流上也位於 S1b 之後，照字面實作會形成循環依賴。

改用**視覺包含**——前一段的 ink 像素有多少比例仍存在於後一段。語意完全
對應「內容單調增加」，且對非文字內容（逐步出現的圖表）同樣有效。
實測 build 的 containment 0.86–0.98，真正換頁 ≤0.56，門檻取 0.70。
詳見 `docs/decisions.md` D8。

### 4.4／4.5（v0.3 移除）

原本是 **S2 OCR**、**S2b 術語詞庫**、**S2c 逐字稿術語校正**——一條以本地
PaddleOCR 為起點的鏈。v0.3 全部移除，職責併入 §4.7 的 S4：

| 原職責 | v0.3 由誰負責 |
|---|---|
| 讀出投影片文字 | **S4a**（`Slide.slide_text`） |
| 建系列術語詞庫 | 不再需要——VLM 直接看得到投影片上的正確寫法 |
| 修正逐字稿同音錯字 | **S4b**（詞庫掃描）＋ **S4c**（模型提案） |
| §5.4 的溯源比對來源 | S4a 的 `slide_text`（v0.4 起 S4a 與 S4c 可用**不同模型**，獨立性部分恢復，見 §2.3） |

**移除的理由**：R8 證明攝影棚幀（81%）不含投影片內容，不需要精確分類；
而本地 OCR 鏈累積了一串與本專案無關的坑（oneDNN 相容性、paddle 版本、
簡繁字集、jieba 詞典是簡體）。VLM 同時看得到投影片畫面與逐字稿，做術語
校正的資訊比詞庫更完整。

**不要為了「備用」把 PaddleOCR 加回來。** 留著就會有人去修它。若日後為了
恢復 §5.4 的獨立性而需要二次 OCR，那是一個要重新決定的設計變更，
`docs/decisions.md` D10／D12／D13 保留了當時踩過的坑。

### 4.6 S3 — 對齊

- **輸入**：`05_transcript.json`、`02_candidates.json`
- **輸出**：`06_segments.json`
- **流程**：
  1. 以候選區段的起訖做**粗切**
  2. 候選區段之間的空隙自成 segment
  3. 把每句逐字稿依其**中點**指派給恰好一個 segment（§5.3 不變量 3）
- **關鍵約束**：這一步**不呼叫 LLM**，避免與 S4a／S4c 形成循環依賴
- **冪等鍵**：`transcript_hash + candidates_hash + s3_params_hash`

**v0.3 移除了語意邊界吸附。** 原設計是「取邊界前後 ±20 秒的句子，計算其
embedding 與前後兩張投影片 OCR 文字的相似度，吸附到轉折點」。它需要
**投影片文字**，而 v0.3 移除本地 OCR 後文字要到 S4 才有；本節又禁止 S3
呼叫 LLM。兩個約束一起使得吸附無法在此進行。

實測上損失有限：真實素材的相鄰投影片語意相似度達 0.873，超過守門門檻，
吸附本來就不會作用（`docs/decisions.md` D11）。但 §5.2 的「對齊邊界誤差
中位數 ≤ 5 秒」目前**無法評估**，見 §9 的 R10。

**`mode` 在此只是暫定**——有候選幀就先標 `slide`，S4a 判定 `is_slide=false`
時會降級為 `speaker_only` 並清掉 `slide_ref`。

### 4.7 S4 — 理解（v0.4 拆為 S4a / S4b / S4c）

> **v0.4 修訂。** 原本 S4 是單一階段，per segment 一次呼叫，同時做
> 「判斷是不是投影片、逐字轉錄、修正錯字、結構化理解」四件事。
> 拆開的三個理由都是實測出來的：
>
> 1. **同一張投影片被重複轉錄。** 全片 49 個 segment 中，
>    《太上老君內觀經》那一張出現 **10 次**，每次都送一遍。
>    實測本地模型在 10 次之間有 5.2% 的字元變異——而 `slide_text` 是
>    §5.4 的比對基準，**基準本身在抖**（`experiments/r14_local_vlm/`）。
> 2. **不同子工作的最佳模型不同**，差距大到不能忽略（見 §2.3 的表）。
>    綁在同一次呼叫裡就只能用同一個模型。
> 3. **批次多模態呼叫會錯位。** 原設計把一批 3 段的文字放前面、圖片
>    裸接在後面，實測 **30.6% 的 segment 拿到隔壁那張圖的分析結果**
>    （`docs/decisions.md` D20）。逐張一次呼叫從根本上不會發生這件事。
>
> 拆開後每張相異投影片只理解一次，額度與時間都下降，
> 且順帶補回 §9 R9 的獨立性缺口（見 §2.3）。

**單位的注意事項**：S4a 的處理單位是**相異投影片**。
去重（S1c）目前是 `docs/proposals/v0.4-transcript-first.md` 的提案，
**尚未納入本規格**。在它落地之前，S4a 的單位退化為「候選幀」——
行為正確但會重複轉錄同一張投影片，第 1 點的問題仍在。

---

#### 4.7a S4a — 投影片理解（逐張一次呼叫）

- **輸入**（per 投影片）：**單一**候選幀圖。**不含逐字稿**——
  這一步只讀圖，避免逐字稿的內容洩漏進 `slide_text`（§5.4 的溯源基準
  必須獨立於待驗證的內容）
- **輸出**：`Slide.slide_text`、`Slide.layout_description`、`is_slide` 判定
- **模型**：**逐子工作可設定**（§2.3）。三項子工作為
  `is_slide` / `slide_text` / `description`
- **批次策略**：**不批次。** 一張圖一次呼叫。D20 的錯位就是批次造成的，
  而 S4a 的呼叫數已因去重大幅下降，批次省不到什麼
- **冪等鍵**：`slide_id + prompt_version + model`
- **失敗行為**：JSON 解析失敗 → 重試 2 次 → 仍失敗則該張 `slide_text` 留空
  並記錄。**不得以部分輸出充數**

**structured output 的欄位順序**：`is_slide → slide_text → description`。
欄位逐一生成、先產生的不能回頭改，這個順序強制模型**先抄再詮釋**。

> **v0.4 的一個未解問題**：實測把 `description` 排在 `is_slide` **之前**
> 時，分類準確率從 76.2% 升到 81.0%（模型先用文字描述看到什麼，再判斷）。
> 但 n=21、只差 2 例，且該組多了一次 JSON 失敗——**不足以下結論**，
> 因此規格維持原順序。見 `experiments/r16_slide_dedup/REPORT.md` §3。

---

#### 4.7b S4b — 詞庫事後校正（純本地，不花額度）

以 S4a 產出的 `slide_text` 建詞庫，掃描逐字稿中「拼音相同但寫法不同」的
片段，產生校正提案。**必須在 S4a 全部跑完之後**——詞庫要從**所有**投影片
建起來，第 5 張投影片上的術語可能在第 20 段被聽錯。

- **詞庫單位**：**標點／空白分隔的完整片段**，不是任意 n-gram。
  實測任意 n-gram（1190 詞）會把投影片某行的碎片當術語，
  於是把**正確的**逐字稿改壞（`一開始`→`以開始`）；改成完整片段（107 詞）
  後，在乾淨的人工字幕上誤報從 19 降到 5，且那 5 筆經人工複核**全部正確**
- **提案不繞過任何把關**：仍走下方 (b) 的三道閘
- **來源標記**：`Correction.method = "lexicon"`，與 VLM 提的分開，
  §5.6 的抽檢要分得出來

見 `docs/decisions.md` D25。

---

#### 4.7c S4c — 逐段理解

- **輸入**（per segment）：
  - 該 segment 的逐字稿
  - 該時段螢幕上的 `slide_text` 與 `description`（由 S4a 產出）
  - 前一 segment 的 `summary`（維持連貫性，不超過 200 字）
- **輸出**：`07_understanding/seg_NNN.json`
- **模型**：可設定（§2.3）。**這一步不需要看圖**——投影片的內容已由 S4a
  文字化。是否仍送圖為設定選項，預設不送
- **批次策略**：可將 2–3 個相鄰 segment 合併為一次呼叫以節省額度，
  但**輸出仍須逐 segment 分開**。
  **不送圖時批次是安全的**——D20 的錯位是「多張圖 + 一段文字」造成的
- **降級模式**：
  - `mode == "speaker_only"` → 沒有投影片可參考，純文字理解
  - `mode == "transcript_only"` → 全片無投影片，退化為逐字稿結構化
  - 額度耗盡 → 停止並記錄進度，**不改用本地模型偷渡**（本地 fallback 須為
    明確的設定開關，見 §5.5）
- **冪等鍵**：`segment_id + prompt_version + model`
- **失敗行為**：單一 segment 失敗 → 重試 2 次（指數退避）→ 仍失敗則標記
  `understanding: null`，繼續下一個 segment

---

#### 子工作的規格（v0.3 新增，v0.4 分配到各子階段）

**(a) `is_slide`——判斷候選幀是不是投影片。**（S4a）

S1b 只知道「這段時間畫面是靜止的」，不知道那是投影片還是講者鏡頭。
`is_slide: false` 時，該 segment 降級為 `speaker_only`、清掉 `slide_ref`，
且不得有 `slide_ocr` 來源的 `content_block`。

prompt 必須警告：**講者所在的攝影棚背景經常有大量裝飾文字**（標語、書法、
招牌），那是佈景不是投影片內容。判斷依據是「這是為了講解而製作的教材」，
不是「畫面上有沒有字」。v0.2 產出「里明品带务务念」正是誤把背板當投影片。

**(b) `corrections`——修正逐字稿的同音錯字。**（S4b 與 S4c 都會產生）

每筆為 `{from, to, reason, method}`。`from` 必須是逐字稿中**實際出現的
字串**，一字不差——對不上的**丟棄並記錄**，不做模糊比對硬套（否則會違反
§5.3 不變量 10）。`reason` 供 §5.6 的人工抽檢判斷改得對不對。
`method` 區分 `vlm`（S4c 提的）與 `lexicon`（S4b 掃的）。

> **v0.4 修訂授權條件。** v0.2/v0.3 寫的是「正確寫法出現在這張投影片上，
> 且與錯字同音或近音」。實測發現**前半段是錯的**：首跑 9 筆校正中
> **只有 1 筆**的正確寫法真的出現在該段的投影片上，而那 1 筆恰好是唯一的
> 插入型違規。原因是 ASR 的同音錯字出現在**講者口述**中，不在投影片上；
> 5/9 甚至來自根本沒有投影片的區段。把這條實作成程式規則會擋掉 4 筆合規
> 校正、放行唯一危險的那筆——**鑑別力是負的**。見 `docs/decisions.md` D19。

**授權條件（v0.4）**：**講者嘴巴發出的音沒變，只是字寫錯了。**

可以改：同音／近音的專名誤寫、音近漏字、ASR 造成的疊字、被截斷的詞。

**不可以改**（各附實測案例）：

| 類型 | 實測案例 |
|---|---|
| 補上講者沒說的字 | `陽神為三魂` → `陽神為三魂，動而生也` |
| 改事實 | `啟示經` → `創世記`、`唐朝` → `宋朝` |
| 換成意思相近的別的詞 | `投胎轉世` → `十個月懷胎` |
| 潤稿與格式化 | `沒sense` → `沒Sense`、刪贅詞 |

**程式端三道閘**（`weft.validation.corrections`）：插入（`to` 包含 `from`
且多出 >2 字）、語意改寫（拼音相似度 <0.35）、大小寫（casefold 後相同）。
實測把首跑那 9 筆的 precision 從 **0.56 提到 1.00**，對照組誤殺 0/15。

**明知擋不到的**：事實修正（`唐朝`→`宋朝` 拼音 0.500、`六祖惠能`→`六祖慧能`
拼音 1.000）與授權樣本在字串層面完全重疊。這是量測後判定**做不到**，
不是還沒做；剩下的防線是 prompt 的負面示例與 §5.6 的人工抽檢。
傷害可逆是因為 `text_raw` 永不覆寫（§4.5 約束 3）。

**套用時的兩個約束**（v0.4，`docs/decisions.md` D22）：

1. `text_corrected` 必須是 `text_raw + corrections` 的**純函數**，
   不得在既有的 `text_corrected` 上疊加——否則續跑會偏移，
   而偏移會寫進磁碟變成新的起點
2. `from` 是 `to` 的子字串時（截斷補全），**不得全域取代**——
   `未`→`未來` 作用在「現在、過去、**未來**」上會產生「未來來」。
   須先標出既有的 `to` 出現位置當保護區

**Prompt 必須明確要求的事項**：
1. 輸出結構化版面描述（箭頭指向、雙欄對應、色彩編碼的意義），不只是文字（S4a）
2. 每個 `content_block` 標註 `type` 與 `provenance`（S4c）
3. 展開所有指涉性語句（「這個式子」→ 指明是哪個）（S4c）
4. **不得推測畫面上與逐字稿中都沒有的資訊**（S4a、S4c）
5. **先逐字轉錄 `slide_text`，再據以詮釋**（S4a）

**多模態輸入的綁定規則（v0.4，D20）**：一次呼叫送多張圖時，
**每張圖必須緊接在它自己的區段標頭之後**，不得「文字全部在前、圖片裸接在
後面」。缺圖時要明講而非靜默跳過——靜默跳過會讓後面的圖遞補上來、整批位移。
S4a 一張一次呼叫，天然不受此影響；此規則主要約束 S4c 若設定為送圖時的行為。

### 4.8 S5 — 全片統整

- **輸入**：`07_understanding/*.json`（**不再讀圖**）
- **輸出**：`08_video.json` 的頂層欄位（TL;DR、術語總表、章節結構）
- **成本**：1–2 次呼叫

### 4.9 S6 — 渲染

- **輸入**：`08_video.json`
- **輸出**：
  - `out/chunks.jsonl` — 產品
  - `out/debug/{video_id}.md` — 人工抽檢用，含內嵌圖片與可點的時間戳連結
- **chunk 切分規則**：一個 `content_block` = 一個 chunk。若單一 block 超過 800 字則按句切分，但**每個切片都要複製完整 metadata**

---

## 5. 驗證

> **本章在實作章節之前，且為實作的前置條件。**
> 驗證框架必須先於功能程式碼建立。§5.5 的禁止事項為硬性規定。

### 5.1 測試資料

**（A）合成影片** — 主要驗證手段

用 ffmpeg 合成，切換時間點由自己指定，ground truth 精確到毫秒、免費、可重現。

必須包含以下**對抗樣本**，缺一不可：

| 編號 | 樣本 | 期望行為 |
|---|---|---|
| A1 | 標準整頁換頁，每頁停留 30–120 秒 | 全部正確偵測 |
| A2 | **逐條動畫**，一頁分 6 次疊加出現 | 偵測為 **1 頁**，keyframe 為內容最完整的版本 |
| A3 | **純講者無投影片**（靜態人像 + 輕微晃動） | 偵測為 **0 頁** |
| A4 | **移動紅點**疊加在靜態投影片上 | 偵測為 **1 頁**（紅點不觸發換頁） |
| A5 | 投影片內嵌播放的短影片 | 不切成數十頁 |
| A6 | 講者頁與投影片頁**交錯**出現 | 分類正確，投影片頁數正確 |
| A7 | **回放**：講者退回前一頁再前進 | 偵測為 3 次切換（非 2 次） |
| A8 | **攝影機緩慢推近**（純講者背景，全程無投影片） | 偵測為 **0 頁**（鏡頭運動不觸發換頁） |
| A9 | **1 秒交叉淡化轉場**進出全螢幕投影片 | 段落邊界正確（±2 秒內），且 **keyframe 不得取到轉場幀** |

**A8、A9 為 2026-08-04 依真實素材實測結果新增。**

理由：A1–A7 的合成素材**無鏡頭運動、無交叉淡化**（見 `docs/decisions.md`
D5），而這兩者正是真實素材上失敗的直接原因——

- **鏡頭運動**：攝影機推近會讓整個畫面相對背景常態位移。實測 `zIglvjoU9vo`
  的 t=199→201、t=554→556 各有一次推近。這是「以畫面變化為基礎的偵測」
  最根本的混淆源，A1–A7 完全沒有涵蓋。
- **交叉淡化**：v0.1 的「取段末幀」在有交叉淡化的邊界會取到混合幀，
  產出 OCR 讀不乾淨的投影片圖。A1–A7 的段落都是硬切，測不出這個問題。

**合成素材全綠、真實素材產出垃圾**，正是因為對抗樣本沒有涵蓋真實素材
才有的現象。新增 A8／A9 是為了讓這類落差在合成階段就被抓到。

**（B）真實影片黃金集** — 現實校準

手工標註 3 支真實影片（含目標 playlist 的至少 2 支）的換頁時間點。標註成本約每支 30 分鐘，一次性投資。

**標註規範**（2026-08-04 補上。沒有這份規範，「什麼算一次換頁」在有交叉
淡化與鏡頭運動的素材上沒有明確定義，標註者之間會不一致）：

1. **一次換頁 = 全螢幕投影片段落的起訖。** 攝影棚畫面內部**不計**換頁——
   講者的動作、手勢、鏡頭推近都不是換頁。
2. **轉場幀的歸屬不需標註。** 1 秒的交叉淡化混合幀落在 ±2 秒容忍窗內，
   標在哪一側都不影響 F1。標註者不必為此糾結。
3. **必須標註攝影機變焦／平移的時刻。** 這些時刻不是換頁，但會讓任何
   以畫面變化為基礎的偵測產生誤報。標出來才能在評估時區分
   「演算法錯」與「素材本來就難」——後者是 §9 的風險項，不是 bug。
4. **標註檔須記錄 `reviewed` 欄位**，未經人工確認者不得用於 §5.2 的驗收。
   機器輔助提案可以，但提案演算法必須與受測演算法不同——用 S1b 自己的
   輸出當 ground truth，測出來必然接近滿分且毫無意義。

### 5.2 量化門檻

**門檻寫死在測試程式碼中，不得在測試失敗時調整。**

| 指標 | 對象 | 門檻 | 備註 |
|---|---|---|---|
| 換頁偵測 boundary F1（容忍 ±2 秒） | 合成影片 | **≥ 0.95** | 環境完全可控，應接近滿分 |
| 換頁偵測 boundary F1（容忍 ±2 秒） | 真實影片 | **≥ 0.75** | 參考：文獻中 naive frame diff 約 0.50–0.60，好方法約 0.81+ |
| speaker/slide 分類 accuracy | 兩者 | **≥ 0.95** | 硬切素材，這題應該很簡單 |
| 逐條動畫合併正確率（A2） | 合成 | **= 1.00** | 這是設計目標，不容失敗 |
| 術語校正 precision | 黃金集 | **≥ 0.90** | 寧可漏改，不可亂改 |
| 術語校正 recall | 黃金集 | 記錄但不設硬門檻 | 首版求穩 |
| 對齊邊界誤差中位數 | 黃金集 | **≤ 5 秒** | |
| 溯源通過率 | 全部 | **≥ 0.95** | 見 §5.4 |

### 5.3 機械式不變量（assert，不需人工判讀）

每次跑完必須全數通過，任一失敗即中止並報錯：

1. segments 時間區間**互不重疊**
2. segments 的聯集**等於影片全長**（容忍 ±1 秒）
3. 每一句逐字稿**恰好**被指派到一個 segment
4. 所有 `slide_ref` 指向存在的 slide 物件
5. 所有 `image_path` 對應的檔案**實際存在**且可開啟
6. 所有時間戳**單調遞增**且在 `[0, duration]` 範圍內
7. 每個 `content_block` 的 `provenance` **非 null**
8. 每個輸出 chunk 的 `metadata` 欄位**完整**（無缺欄、無 null）
9. `transcript_raw` 在任何階段後**未被修改**（hash 比對）
10. `corrections` 中每一筆的 `from` 字串**實際出現**在 `transcript_raw` 中

### 5.4 溯源檢查（防幻覺閘門）

這是防止 LLM 編造內容的**唯一有效機制**，必須實作。

**正向檢查**：對每個 `content_block`，取其 `provenance` 指向的來源文字，
計算 block 內容與來源的相似度。

來源為何（v0.3 起）：

| `provenance_kind` | 來源 |
|---|---|
| `slide_ocr` | `Understanding.slide_text`——**S4 的 VLM 讀出的**投影片文字 |
| `transcript` | 該時段的逐字稿 |

> ⚠️ **v0.3 起本檢查失去了獨立性。** v0.2 的來源是本地 OCR，與 LLM 是兩個
> 系統；v0.3 的來源是 VLM 自己讀出的文字。模型若系統性誤讀投影片，
> `slide_text` 與 `content_blocks` 會**一致地錯**，本檢查全數通過。
>
> 已實作的部分緩解是 §4.7 的欄位順序（先抄後詮釋），它擋得住「詮釋時越說
> 越多」這種最常見的幻覺，擋不住「一開始就讀錯」。
>
> **這是保護強度的實質下降，不是等價替換。** 補救選項見 §9 的 R9。

- 相似度 < 門檻 → 標記為 `unverified`
- 每支影片 `unverified` 比例 > 5% → 整支標記為 `needs_review`，不進入 `chunks.jsonl`
- 所有 `unverified` 條目寫入 `out/debug/unverified.jsonl` 供人工檢視

**反向檢查（同樣重要）**：若 `content_block` 與來源的相似度**過高**（例如 > 0.98 的逐字複製率超過 80% 的 blocks），代表 LLM 只是在複製貼上而沒有理解與整合。

- 這同樣是失敗，記為 `degenerate_copy`
- 設計上，`經文原文` 型別**應該**是高複製率（那是引文），`白話解說` 與 `口頭延伸` **不應該**是。檢查須依 `type` 分別設定範圍。

**具名實體檢查**：`content_block` 中出現的人名、書名、數字、年代，必須在來源文字中出現。不在的一律標記 `unverified`。

### 5.5 禁止捷徑清單

以下行為在本專案中**明文禁止**。若實作者（含 AI coding agent）認為某項規定阻礙進度，正確做法是**停下來提出討論**，而不是繞過。

**關於偵測**

1. 不得以**固定間隔取樣**（每 N 秒抓一張）冒充換頁偵測。
2. 不得跳過 speaker/slide 分類，直接把所有幀當作投影片。
3. 不得只用逐字稿讓 LLM「猜」投影片邊界或投影片內容。

**關於理解**

4. 不得產生任何**無法溯源**的具體事實（人名、數字、引文、年代）。
5. 不得為了通過溯源檢查而讓 LLM 只做複製貼上（反向檢查即為此設）。
6. 不得在 S4 額度耗盡時**靜默**改用本地模型。本地 fallback 必須是明確的設定開關，且輸出須標記 `model_used`。

**關於測試**

7. **不得為了讓測試通過而調低 §5.2 的門檻**。門檻是設計目標，不是可調參數。
8. **不得縮小測試集**或移除對抗樣本（§5.1 的 A1–A9 為必選）。
9. 不得把 §5.3 的 assert 改成 warning 或 log。
10. e2e 測試**不得用 mock 取代真實模型呼叫**。單元測試可以 mock，但檔名須含 `_unit_`，且 e2e 測試須獨立存在並實際跑通至少一支影片。
11. 不得在測試中使用「跑過就好」的斷言（如 `assert result is not None`）取代量化門檻。

**關於額度**

12. 不得嘗試以任何方式將 Google AI Pro/Ultra 的**訂閱額度**接入程式化呼叫。額度來源固定為 Gemini API key。
13. 不得靠撞 429 來探測額度上限；必須主動用 quota ledger 預估與節流。

### 5.6 人工抽檢協議

自動檢查無法涵蓋「內容是否真的有用」。因此：

- 每批次跑完後，從 `out/debug/` 隨機抽 **3 個 segment** 人工檢視
- 檢查項目：投影片圖是否為該頁最完整版本、逐字稿術語是否正確、`content_block` 分類是否合理、chunk 是否自足
- 結果記入 `out/review_log.md`（日期、抽檢 segment、通過/問題、處置）
- **連續兩批出現同類問題 → 停止批次處理，回頭修 pipeline**

---

## 6. 額度與續跑機制

### 6.1 Quota Ledger

`out/quota.db`（SQLite），記錄：

```
(timestamp, model, request_count, input_tokens, output_tokens, segment_id, status)
```

**主動節流**：每次呼叫前估算 token 用量，與當日已用量相加，超過安全水位（設為配額的 90%）即停止本日處理。**不靠撞 429**——429 會讓做到一半的 segment 白費。

### 6.2 重置時間

Gemini API 的 RPD 於**太平洋時間午夜**重置。換算台灣時間：

- 夏令時間（PDT, UTC−7）→ **15:00**
- 冬令時間（PST, UTC−8）→ **16:00**

排程須依此設定，**不是台灣午夜**。實作上直接用 `zoneinfo` 以 `America/Los_Angeles` 計算，不要寫死時差。

### 6.3 續跑語意

- 每支影片的 `state.json` 記錄各階段完成狀態與**參數 hash**
- 參數 hash 變更 → 該階段及其下游需重跑；上游不動
- S4 以 segment 為粒度斷點（`07_understanding/` 每 segment 一檔）
- 重啟時掃描 `work/`，重建待辦佇列，從中斷處繼續

### 6.4 Producer / Consumer 分離

兩個獨立可執行的入口：

- `pipeline survey <playlist|video>` — 跑 S-1（§4.0）。
  **v0.4：逐支跑**，並更新系列彙總。`prepare` 會自動帶跑，
  這個入口是為了單獨檢視 profile 而保留
- `pipeline prepare <playlist>` — 跑 S0 → **S-1** → S1a/S1b → S3，
  不受額度限制，可一次跑完整個系列
- `pipeline understand` — 跑 S4a → S4b → S4c → S5 → S6，
  消化 buffer 直到額度耗盡，自動停止。
  **S4b 純本地不花額度**；S4a/S4c 是否花額度依 §2.3 的模型配置而定

這讓額度重置時工作已就緒。

### 6.5 預算估算（供驗收參考）

以 42 分鐘影片估算。**v0.4 起這一節依 §2.3 的模型配置而變**——
子階段設為本地模型時該階段不花額度。

**全部走雲端模型時**（v0.3 的配置）：

| 階段 | 呼叫數 | 說明 |
|---|---|---|
| S4a | 每張候選幀 1 次 | 實測 49 張；**去重（S1c）落地後降到 11 張** |
| S4b | 0 | 純本地 |
| S4c | 3 segment/呼叫 → 約 17 次 | |
| S5 | 1–2 次 | |
| **合計** | **≈ 67 次**（去重後 ≈ 30 次） | |

> **v0.3 的原估算是錯的。** 原文寫「S4：3 segment/呼叫 → 約 10–12 次，
> ≈14 requests/支」，並假設 free tier 是 **1,000 RPD**。
> 實測 `gemini-3.1-flash-lite` 的 free tier 是 **20 RPD**——差 50 倍。
> 實跑一支 42 分鐘影片用掉 17 次，占當日配額 85%。
> 「每日 15–50 支」的預期與現實差了兩個數量級。
>
> 額度上限**不得寫死**，由 §6.1 的 ledger 從 429 回應學習
> （`docs/known-risks.md` 的實作紀錄）。

**S4a 走本地模型時**：全片 VLM 時間約 1 分鐘（qwen2.5vl:7b）到
7 分鐘（gemma4:12b），**無額度上限**。實測見 `experiments/r21_bakeoff/`。

若實測呼叫數顯著高於此，先檢查是否 CV 前置過濾失效（候選幀過多）。

---

## 7. 分期範圍

### 7.1 Phase 0 — 骨架與驗證框架（先於任何功能程式碼）

- 專案結構、設定檔、logging
- IR schema 的 dataclass / pydantic 定義
- **合成測試影片產生器**（A1–A9）
- §5.3 的不變量檢查器
- 空的 e2e 測試（跑不通，但存在）

**完成條件**：跑 `pytest` 會失敗，但失敗訊息清楚指出缺少哪些實作。

### 7.2 Phase 1 — 本地管線（S-1、S0–S3，無 LLM）

- **S-1 素材勘查**（§4.0）：**逐支**的 profile 與不符偵測（v0.4）
- yt-dlp 取得、Whisper、抽幀、ink Jaccard + HMM 靜止區段、逐條動畫合併
- 對齊（粗切）

**完成條件**：`06_segments.json` 產出，且 §5.2 中所有不涉及 LLM 的門檻通過。此時人已經可以打開 `03_slides/` 和 segments 檢查品質。

### 7.3 Phase 2 — 理解（S4a／S4b／S4c、S5）

- structured output、prompt、批次策略
- **逐子階段的模型配置**（§2.3）與序列化載入（v0.4）
- **S4b 詞庫事後校正**（純本地，v0.4）
- quota ledger、續跑
- 溯源檢查

**完成條件**：單支影片端到端產出 `08_video.json`，溯源通過率 ≥ 0.95。

### 7.4 Phase 3 — 批次與輸出（S6）

- `chunks.jsonl`、debug markdown
- playlist 批次、失敗處理、review log

**完成條件**：目標 playlist 完整跑完，人工抽檢通過。

### 7.5 v2（本次範圍外，但 schema 須預留）

- 系列級 concept registry（跨影片概念合併、增量更新）
- 多模態檢索
- Obsidian 輸出

**schema 預留**：`chunks.jsonl` 的 metadata 已含 `series_id` 與 `episode_index`，v2 可直接在其上建索引，不需重跑。

---

## 8. 環境

- **OS**：Ubuntu 24.04
- **GPU**：RTX 5070 Ti（16GB GDDR7, sm_120 Blackwell）
- **隔離**：conda

**兩個環境**：

| 環境 | 用途 | 內容 |
|---|---|---|
| `pipe-cpu` | S-1, S0, S1b, S3, S4–S6 | yt-dlp, opencv, numpy, pydantic, google-genai |
| `pipe-gpu` | S1a（Whisper）, S4 fallback | torch, faster-whisper |

分開的理由：除了 Whisper 與本地 fallback，v0.3 已沒有任何階段需要 GPU——
S1b 是純 CV，S3 只做粗切（不再需要 embedding），S4–S6 是 API 呼叫。
有手動字幕的影片（例如目標素材）**全程不需要 GPU**。

**兩個已知的雷**：

1. **PyTorch 必須從官方 index 用 pip 安裝**，不要用 conda-forge 版本。Blackwell 是 sm_120，conda 版容易裝到不支援的建置，錯誤訊息是 `no kernel image is available for execution on the device`，很難查。conda 只負責 Python 版本與環境隔離。
2. **ffmpeg 用系統 apt 版本**，不要用 conda 的——conda 版編碼器支援常有缺漏，抽幀會出現難以解釋的行為。

---

## 9. 開放問題與風險

| 項目 | 風險 | 緩解 |
|---|---|---|
| **§5.4 溯源檢查失去獨立基準** | v0.3 的比對來源是 VLM 自己讀出的 `slide_text`。模型若系統性誤讀，來源與內容會**一致地錯**，檢查全數通過 | §4.7 的欄位順序（先逐字轉錄再詮釋）擋得住「越說越多」，擋不住「一開始就讀錯」。**保護強度實質下降**。補救選項見 `docs/known-risks.md` R9 |
| **素材與 profile 不符** | §1.3 是單支影片的實測結果。**同系列其他影片**的畫面結構也可能不同——v0.1 栽在「單支→系列」，v0.3 又栽在「抽樣→系列」（實測第 14 集換了攝影棚背景） | **S-1 素材勘查**（§4.0）**逐支**強制執行（v0.4），不符即中止並報告 |
| **對齊邊界誤差無法評估** | v0.3 移除了語意吸附（§4.6），§5.2 的「誤差中位數 ≤5 秒」目前沒有東西可測 | 黃金集（§5.1 B）標註後才能量。實測損失有限（相鄰投影片相似度 0.873，吸附本來就不作用），但**未量到不等於沒有損失**。見 R10 |
| **文言文 ASR 無前置緩解** | v0.3 移除了餵給 Whisper 的系列術語詞庫（詞庫來自本地 OCR） | 錯字改由 S4 事後修正。目標素材有手動字幕不受影響；無字幕影片尚未實測。加回來不需恢復整條 OCR 鏈——可用同系列已處理影片的 `Understanding.terms` 建表。見 R11 |
| **鏡頭運動** | 攝影機推近／平移會讓整個畫面相對背景常態位移，是「以畫面變化為基礎的偵測」最根本的混淆源 | 二分類已實測不受影響（變焦幀距門檻 8.3–10.2x，見 §4.3 步驟 2）；**A8 為必測對抗樣本**；黃金集須標註變焦時刻以區分「演算法錯」與「素材難」 |
| **二分類的全螢幕側餘裕窄** | Otsu 門檻與全螢幕最小值只差 1.01x，因為轉場幀在兩類之間形成連續帶 | S-1 量測模式分離度，低於 3x 時須人工檢視 |
| 文言文 ASR | Whisper 對文言文的表現未知 | 這正是術語校正存在的理由。若 raw 錯誤率過高，考慮把系列詞庫餵進 `initial_prompt`。**本片有手動字幕，未觸及此風險**；無字幕的影片仍未實測 |
| Gemini 免費額度變動 | Google 曾大幅調降免費額度 | Ledger 讀取實際配額而非寫死；模型設定可替換 |
| 逐條動畫的判定 | 視覺包含的規則可能誤判 | A2 為必測樣本；若真實影片表現不佳，退回保守策略（寧可多切，人工合併） |
| 版權 | 素材為他人著作 | 個人知識庫用途，不對外散布。輸出保留 `url` 與時間戳，便於回到原始出處 |

**已解除的風險**（2026-08-04 實測，保留紀錄）：

| 原風險 | 實測結果 |
|---|---|
| 直排文字 OCR 準確度未實測 | PaddleOCR 對六種版型信心 **0.98–1.00**，直排經文六行全對。**v0.3 已移除本地 OCR**，此風險連同 PaddleOCR 的一串坑（oneDNN、paddle 版本、簡繁字集）一併消失 |
| Blackwell sm_120 的 PyTorch 支援 | `torch 2.11.0+cu128` 從官方 index 安裝，在 sm_120 上實跑 matmul 通過 |
| 「半透明疊加」是否為第三種模式 | **否。** 逐秒檢視 2519 幀確認為 1 秒交叉淡化轉場，全片 10 幀（0.40%）。IR 不需第三種 mode（`experiments/r8_overlay/REPORT.md`） |

---

## 附錄 A：參考實作

| 專案 | 可借鑑之處 | 注意 |
|---|---|---|
| `patrickmineault/vid2slides` | ffmpeg 抽幀、滿版人臉過濾、HMM 換頁、ROI 裁切 | 2020 年程式碼，環境已不可用。**移植演算法，不 fork** |
| `HHousen/lecture2notes` | 九步驟 pipeline 架構、透視裁切、圖表抽取 | **AGPL-3.0**，只讀不抄 |
| `SliTraNet`（arXiv 2202.03540） | 換頁偵測評估方法、F1 基準值 | 用於校準 §5.2 的門檻 |
| Video-RAC | 依投影片邊界做 adaptive chunking 優於硬切的實證 | 支持本設計的 chunk 策略 |

## 附錄 B：術語

- **IR**：Intermediate Representation，本系統的中介表示（§3）
- **segment**：一張投影片 + 其對應逐字稿所構成的時間區間
- **build**：逐條動畫中的一個中間狀態
- **溯源**：content_block 可回推到 slide OCR 或逐字稿的性質
