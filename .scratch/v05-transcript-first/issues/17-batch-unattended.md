# 17 — 無人執行：整批跑一次播放清單

**要做出什麼：** `weft prepare <playlist>` 能無人跑完 26 支，而且**回報可信**。

**Status:** done（2026-09-01）

**來源：** 使用者定的 v1 收尾條件。理由是——中醫四支有**人工字幕**，
所以「逐字稿是可靠資產」在那批是免費的；**沒字幕的素材（機器人學）
才是 Whisper 那條路唯一被驗的機會**。

## 第一次跑就撞到三層

| 層 | 問題 | 狀態 |
|---|---|---|
| 1 | 23/26 下載 403，卻印「**26 支目標，全部成功**」 | ✅ 修好，645 passed |
| 2 | yt-dlp 2026.07.04 的 `ANDROID_VR` client 被 YouTube 403 | ✅ 升 2026.08.19（`visionos`）|
| 3 | `pipe-cpu` 沒有 faster_whisper → **沒字幕的全掛在 S1a** | ✅ 改用 `pipe-gpu` + 錯誤訊息指路 |

**第三層兩週都沒現形**，因為中醫四支有人工字幕，`pipe-cpu` 跑得很順。
**換一批素材立刻現形。**

## 已經留下的守衛

- `tests/test_unit_batch_reporting.py` 5 條——含「全部已在 skip list 時
  重跑也不得回報成功」
- `transcribe.py` 的 ImportError 改成指路（D29 那條紀律）
- `docs/FROZEN.md` F10 與 `CLAUDE.md` 記了「403 先升 yt-dlp」，
  並寫明症狀會騙人（**metadata 拿得到、媒體下載才 403**）

## 結果（2026-09-01 18:41 完成，耗時約 95 分鐘）

```
prepare 完成：26 支目標 → 26 成功、0 不可用、0 已在 skip list、0 失敗
```

| | |
|---|---|
| S0–S3 五階段 | **26/26 全部 done** |
| 逐字稿 | **45,553 cue、404,247 字**（Whisper large-v3）|
| 分段 | 1,335 段 |
| `series_id` / `episode_index` | **26/26 全部回填** ← 票 15 的修正第一次被真的用到 |
| 磁碟 | 用掉 30 GB，剩 87 GB；`.mp4` 佔 6.6 GB |

**原料量對照**：中醫四支 19,697 字（人工字幕）→ 機器人學 26 支
**404,247 字**。使用者的框架是「v1 的產出是給 v2 重做用的原料，
真資產是逐字稿」，依那個框架看，**原料量翻了 20 倍**。

### 誠實回報第一次通過完整驗收

同一行訊息，上午印「23 支失敗」、下午印「26 支成功」——**兩種結果都印對了**。
那才叫可信。若只驗過成功的那次，等於沒驗。

### 一個我自己的判準寫錯

驗證時我寫「所有階段都要 done」，於是 `jgVD7IPNTs8` 與 `xBfyWwYylSA`
被判成失敗——**實際上它們五階段全 done**，只是先前手動跑過 S4，
額外帶了 `S4a`/`S4`/`S5`/`S6` 的紀錄，而 S5/S6 是 `pending`。

`prepare` 只負責 S0–S3。**是驗證腳本的判準錯，不是 pipeline 有問題。**

## 沒做

- **S4–S6 沒跑**（約 15 小時）。使用者的判斷：那是會被 v2 丟掉的視圖，
  原料收進來就夠了。
- **產出品質沒確認**——使用者原話：「整個流程是可以跑通的，
  但是我還沒有確認說他做出來東西是怎樣」。**這是下次的第一件事。**
- `.mp4` 6.6 GB 後續用不到（S4 只需要 `03_slides/` 與逐字稿），
  **但沒有刪**——不可逆，而且重跑 S1b 抽幀就要重新下載。

## 給下一個人的操作

```bash
# **一定要用 pipe-gpu**——pipe-cpu 沒有 faster_whisper，沒字幕的影片會全掛
conda run -n pipe-gpu weft -c configs/local.yaml prepare "<playlist URL>"
```

下載全部 403 時**先升 yt-dlp**，不要先懷疑網路或環境（我懷疑錯過一次）。
