# weft — 共用語言

這個檔案是詞彙表。寫規格、票、commit 訊息、變數名時用這裡的詞，**不要發明同義詞**。

新詞進來之前先問：**這個符號會不會被兩個地方用成兩種意思？**
這個 repo 出過的事故全部是這個形態（見 `docs/agents/domain.md`）。

---

## 管線階段

階段一律用代號稱呼，不要用「第幾步」。

| 代號 | 名稱 | 做什麼 |
|---|---|---|
| **S-1** | 素材勘查 | 逐支量測畫面型態，回答「這一支能不能用現有設計處理」。**不產生知識庫內容** |
| **S0** | 取得 | 下載影片與字幕 |
| **S1a** | 逐字稿 | 手動字幕優先，無則 Whisper |
| **S1b** | 投影片候選幀 | CV 找靜止區段 |
| **S1c** | 投影片去重 | 相同投影片分群 |
| **S3** | 分段 | v0.5 起由逐字稿驅動（原為「對齊」，由投影片切換驅動） |
| **S4a** | 投影片理解 | 逐張讀出 `slide_text` 與版面描述 |
| **S4b** | 詞庫事後校正 | 純本地，不花額度 |
| **S4c** | 逐段理解 | 產出 `content_block` |
| **S5** | 全片統整 | |
| **S6** | 渲染 | 產出 `chunks.jsonl` |

---

## 中介表示

| 詞 | 意思 |
|---|---|
| **Slide** | 一張相異的投影片。帶 `occurrences`、`duplicate_of` |
| **Segment** | 一個時間區段。v0.5 起邊界由語意決定，不由投影片切換決定 |
| **Understanding** | S4c 對一個 Segment 的理解結果 |
| **content_block** | Understanding 裡的一個內容單元，帶 `provenance` |
| **Chunk** | 輸出到 `chunks.jsonl` 的一行。**必須自足** —— 不依賴前後文即可理解 |
| **profile** | S-1 對**單支影片**的量測結果。逐支產生，**不是系列通則** |

---

## 驗證詞彙

| 詞 | 意思 | 避免 |
|---|---|---|
| **溯源檢查** | §5.4，比對 content_block 與其來源。**防幻覺的唯一有效機制** | 「驗證」（太廣） |
| **per-video 閘門** | 單支影片 unverified > 5% → `needs_review`，不進 `chunks.jsonl` | 拿它當全域驗收數字 |
| **全域溯源率** | 跨影片的合計統計。**只記錄，不設門檻** | 叫它「通過率」而不指明範圍 |
| **不變量** | §5.3 的十條機械斷言，每次跑完必須全過 | |
| **歸屬錯** | 內容是真的，但 `provenance` 標到另一個來源。**不是幻覺** | 混進「未通過」一起算 |
| **懸空門檻** | 存在於 `ACCEPTANCE_THRESHOLDS` 但沒有被任何非常數斷言使用 | |
| **保留集** | 調校時完全沒碰過的標註集。`GoldenAnnotation.holdout` | 「測試集」（本專案 test 另有所指） |
| **包裝** | 片頭字卡、系列名稱、主講人學經歷、佈景標語、片尾訂閱。**不進知識庫** | 「雜訊」（太廣） |
| **假邊界** | 因鏡頭切回講者而產生的分段邊界。**不是內容邊界** | |

---

## 兩個容易搞混的區別

**「檢查存在」≠「檢查有效」，而前者會偽裝成後者。**
`assert T.X == 0.90` 會綠燈，但它只證明常數沒被改過（D30）。

**「偏離」≠「處理不了」。**
S-1 的中止判準問的應該是後者。純螢幕錄影嚴重偏離 §1.3，
但那是**更好處理**的情況。

---

## 素材

| 詞 | 意思 |
|---|---|
| **系列** | 一個播放清單。**系列內部不保證齊一** —— 第 14、27 集換過攝影棚背景 |
| **有投影片素材** | 講者 + 投影片。中醫講經、機器人學課程 |
| **無投影片素材** | 訪談、口播、動畫解說。v0.5 起管線需自然退化而非中止 |
