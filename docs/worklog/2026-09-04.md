# 2026-09-04 — v1 封版：交付的東西換了一個，而那推翻了前五天的優先順序

接續 [2026-09-01](2026-09-01.md)。這一天沒有寫新功能，做的是**界定**——
而界定完之後，前五個工作天裡相當一部分的工作被歸類成「優化一個註定要丟掉的東西」。

用 `/grill-me` 走了兩輪、六題，使用者六題全部拍板。

---

## 1. 六個決定

| 題 | 決定 |
|---|---|
| **Q1 交付物** | **逐字稿層 S0–S3**，不是 `chunks.jsonl` |
| **Q2 契約** | 產物導向——v1 = 這 31 支的產出，不是通用工具 |
| **Q3 之後** | 封版停數週；清 `02_frames` 36 GB，留 `.mp4` |
| **Q4 第三層終點** | **訓練模型**，RAG 是沿路的驗證手段 |
| **Q5 驗收** | 機械 4 條 **＋** 人讀抽樣（明標非量化）|
| **Q6 出界的東西** | 分類紅燈與 351 筆 chunk **原封不動留著**，寫明地位變了 |

決策記錄 D35／D36，完整內容 `docs/v1-scope.md`。

### 為什麼是逐字稿層

| | 逐字稿層 | 理解層 |
|---|---|---|
| 覆蓋 | **31/31** | 8/31 |
| 確定性 | 是 | 否（CV 0.34，跨 process 不可重現）|
| v2 會不會丟掉 | **不會，是原料** | **會** |

補跑那 23 支的 S4–S6 約 15 小時，而它**不會改變三件已知的事**
（2.3 倍變異、21.2% 轉述框架、48.4% 是沒標點的逐字稿），
只會把它們乘以 4 倍。

> 8/08–8/13 的力氣大量花在 S4c 行文品質與溯源通過率上。
> **依這個定義，兩件在 v1 裡都不重要。**
> 記下來不是自責，是路標：**先定交付物，再定要優化什麼。**

## 2. 驗收：機械那半

`tests/test_v1_archive.py` 4 條，基線 `tests/golden/v1_archive_manifest.json`
（2026-09-04 實測）。三條分開寫是刻意的：

- `integrity` 問「**在場的**對不對」——逐支比對，不是比總和
  （總和會互相抵銷：一支掉 200 字、另一支多 200 字，加起來看不出來）
- `is_complete` 問「**是不是全都在**」——只有前者的話，`work/`
  被砍到剩三支也會全綠
- `prepare_stages_done` **只驗 S0–S3**——9/1 的驗證腳本正是寫成
  「所有階段都要 done」，把兩支帶 S5/S6 `pending` 的影片誤判成失敗

**照 R37 驗過它會紅**：故意把一支字數 +1、加一支不存在的影片，
兩條分別紅了、訊息指到正確的那一支。

## 3. 驗收：人讀那半——27 支全部讀過

沒有人工字幕，**算不出 CER**，所以只能人讀、而且結論要標成非量化。

### 機械訊號先排除災難（這些是量化的）

| 指標 | 27 支的值 |
|---|---|
| 字/秒 | 4.16–5.27，中位 4.72——**全距很窄，沒有一支崩掉** |
| 最大 6-gram 重複 | 2–15，是講課口頭禪，不是 Whisper 的重複迴圈 |
| 全形標點密度 | **0.00%–0.17%，等於沒有標點** |
| 非中英字元 | 查過，幾乎全是半形 `?`，其餘 θ／α／β |

### 人讀的結論

27 支全部是連貫、切題、看得懂的中文。**而錯誤幾乎全屬同一類**：

| 逐字稿 | 應為 |
|---|---|
| `卡住的關係`／`卡坐標系` | 卡氏座標系 |
| `ControlRaw` | Control **Law** |
| `Inverse Jacobian Collision Control` | …**Compliance** Control |
| `Homogenetic Transformation` | Homogene**ous** |
| `determined j` | determinant J |
| `Effective Inertial` | Effective Inert**ia** |
| `彈音係數` | 彈簧係數 |
| `Learning my demonstration` | Learning **by** demonstration |
| `你的城市`（重複多次）| 你的**程式** |

**全部是專有名詞被聽成音近的非術語。**
這張表就是使用者第二層構想的實證基礎——**詞表就能修，不需要完整語料庫。**

## 4. 一個難堪的發現（R39）

糾錯機制**早就存在**（S4 拿投影片對照修同音錯字）。實測成績：

| | 支數 | corrections |
|---|---|---|
| 人工字幕 | 4 | **16** |
| Whisper | 27 | **0** |

**方向完全反了。** 乾淨的那批修了 16 筆，該修的那批一筆都沒有——
因為它們只跑到 S3，而**糾錯掛在 S4**。

> **糾錯掛錯了層。它掛在「理解」，而它修的是「逐字稿」。**

我一開始把這件事講成「糾錯那層在這批素材上等於沒作用」——**那句推論是錯的**，
當場更正：不是沒作用，是**還沒走到**。差別很重要，因為前者會讓人去改糾錯的
實作，後者指向的是它掛的位置。

## 5. 清 36 GB 之前先補的一行（D36／R38）

`02_frames` 的唯一下游是 `dedup.py` 的 `studio_distance` 診斷，
而它**算不出來就靜靜不寫任何欄位**——讀 `04_dedup.json` 的人分不出
「沒有 frames」與「距離剛好沒被記錄」。**與 D34 的 `video_pass_rate`
是同一類**。已改為明寫「未計算」。

查的過程順帶撞到一個更廣的洞：**`mark_stale`（state 說完成、產物卻不在）
在整個 repo 裡只被呼叫一次**，守 S4a。S1b／S1c 用的 `is_satisfied()`
只比對 `params_hash`，產物在不在它不看。這次沒變成災難（frames 的下游
只有那個診斷），**但修法要先有「哪些產物可拋」的分類，而那不存在——推到 v2**。

### 刪除當下被擋，後來由使用者執行——而執行時出了事

`rm -rf work/*/02_frames` **被權限分類器擋下**，我沒有繞過，留給使用者決定。
使用者稍後自己執行了，**但刪掉的範圍比預期大**——見 §9。

## 6. 使用者提的三層架構

> 「第二層是想辦法（爬蟲？）建一個這個主題的**有相對文字的先驗資料庫**，
> 再根據它去調整逐字稿。第三層才做成 RAG 或小模型的訓練資料。」

方向與文獻一致（RAG-ASR correction／contextual biasing／forced alignment
的共同點就是**修逐字稿、不修驗證**），也直接回應了 8/13 的架構批評。
四個銳化的點寫進 `v1-scope.md` §5.1，最重要的是第一個：

> **RAG 容忍雜訊，訓練不容忍。** 強 reader 自己會把
> `Learning my demonstration` 讀成 `by`；訓練會把它變成模型的信念。
> **因為終點是訓練，第二層是必要的，不是可選的優化。**

---

## 收工狀態

| | |
|---|---|
| 測試 | 乾淨機器 **642 passed / 8 skipped / 0 failed**（19 秒）；有 `work/` 時 **649 passed / 1 failed** |
| 紅燈 | 仍只有分類那條，**四個數字未變**（0.909／0.947／1.000／0.864）|
| 新增 | `tests/test_v1_archive.py` 4 條 + `tests/golden/v1_archive_manifest.json` |
| 文件 | `docs/v1-scope.md`（新）、D35／D36、R38／R39、CLAUDE.md |
| 磁碟 | `work/` **44 GB → 9.1 GB**（`02_frames` 已清）|
| **事故** | 誤刪整個 `work/`，**已從垃圾桶完整還原**（31/31 與基線相符）；揭穿測試漏洞 R40 |

## 下一次

**v1 已封版。** 動任何東西之前先讀 `docs/v1-scope.md`——尤其 §4，
那裡列的東西**不是待辦，是刻意留著的**。

v2 開工時第一件事：**詞表**（`stages/lexicon.py` 已經在），
素材是 `03_slides` 的 1.4 GB，不需要先去爬。
一定要防的陷阱寫在 §5.1 最後：**不能用同一個先驗庫既修又驗**。

## 7. 更正一個一直寫錯的數字：這個專案不是「兩週」

使用者問「這專案做的超久，結果又沒做出什麼東西」。查 git 才發現
**「兩週」這個說法從頭到尾是錯的**，而我在 README 與多份文件裡跟著寫。

```
第一個 commit  2026-08-04
最後一個       2026-09-04        跨度一個月
100 個 commit，分佈在 —— 7 個日期

8/04(19)  8/06(18)  8/08(30)  8/09(19)  8/13(7)  9/01(3)  9/04(4)
```

**實際工作天是 7 天。** 「做超久」的感覺來自日曆上的空白，不是投入的時間。

7 天的產出：`src/` 8,163 行、649 條測試、**35 個實驗／27 份完整報告**、
36 條決策、39 條風險、31 支影片 428,725 字。

已更正 README（改成實測的工作天數與 commit 分佈）、`decisions.md` D35、
`v1-scope.md` §1。**`2026-09-01.md` 裡的 7 處「兩週」沒有動**——
那是當天寫下的紀錄，改動過去的工作日誌比留著一個鬆散的措辭更糟。
這一節就是它的更正。

> 這與 §4 是同一類錯誤：**一個沒有被核對過的數字，被反覆引用之後
> 看起來就像事實。** 兩次都是在寫給別人看的文件時才被抓到。

## 8. 把最大的收穫寫下來——而寫的過程抓到我自己的一個過度推廣

使用者：「這才是我們最大的收穫。然後 Whisper 的邊界也要寫進去。」

寫進 `v1-scope.md` §3.3（Whisper 的邊界）與 §5.1（流程的形狀），
README 也加了一節。**而寫的時候抓到我在對話裡講了兩次的一句錯話。**

我一直說「解碼時注入比解碼後好，DocWhisper 的 14.3% 在那一層」。
**這個 repo 自己量過，結論相反**——`stages/lexicon.py` 的檔頭寫著 R20：

| 插入點 | 實測 | 判定 |
|---|---|---|
| Whisper `initial_prompt`（全域）| 術語 4/19 → 5/19，CER 12.3% → 12.0% | **沒用** |
| **事後校正**（S4b，**已實作**）| 拼音門檻 0.90 下 **precision 100%** | **可用** |
| 逐段 `hotwords` | — | **未測** |

D33 把 R20 改寫成「**全域** prompt 沒用」，那是說**這條路沒被關死**，
**不是說它比較好**。DocWhisper 的 14.3% 是**別人的素材上的別人的數字**。

> **我拿一篇論文的數字，蓋過了這個 repo 自己的實測。**
> 而 CLAUDE.md 第一條紀律就是在防這件事。已全部更正。

另外兩個查出來的事實改變了流程圖：

- **`slide_text` 由 S4a 產生，而 S4a 只跑過 8/31 支。** 那 23 支要拿投影片
  當詞源，得先對 **3,134 張投影片**跑一次 VLM——**這不是免費的。**
- **投影片數極不均**：中位 112、範圍 1–226，**3 支少於 20 張**。
  投影片這個詞源在那 3 支身上不成立。

### 使用者口述的流程，四處經量測修正

| 使用者說的 | 修正 |
|---|---|
| 用逐字稿去找爬蟲目標 | **雞生蛋**——逐字稿裡錯的正好是要搜的詞。詞源順位改成 標題 → 投影片 → 爬蟲 |
| 爬完之後調整逐字稿 | 插入點要用**已實測的 S4b 事後校正**，不是未驗證的解碼層 |
| （沒提到 S4b 已存在）| 機制早就有，**只是掛錯層**（掛在 S4，而那 27 支只跑到 S3）|
| 有逐字稿就直接進資料庫 | 只免除 ASR 那類問題。中醫 4 支走這條路，仍有 21.2% 轉述框架、48.4% 無標點 |

## 9. 收工前一小時的事故：36 GB 變成整個 `work/`

使用者自己執行了 D36 的刪除，回報「我已經把那個 36 G 刪掉了」。
我去確認狀態，發現 **`work/` 整個空了**——被丟進垃圾桶的是
**31 個完整的工作目錄**，也就是 v1 的交付物本身。

**而測試跑起來是 `642 passed / 8 skipped / 0 failed`。全綠。**

31 個目錄完整地在垃圾桶裡（`gio trash --restore "trash:///<vid>"`），
全部還原，逐字稿與基線**一字不差**：

```
31 支中：逐字稿完整且與基線相符 31 ／ 缺 0 ／ 不符 0
01_video.mp4 31/31 · 03_slides 31/31 · 06_segments.json 31/31
02_frames 1/31        ← 這部分確實是照 D36 的原意清掉的
```

還原後：驗收 4/4 通過，完整測試 **649 passed / 1 failed**，
紅燈四個數字未變。`work/` 從 44 GB 變成 9.1 GB。

### 而測試為什麼沉默——R40

當天早上才寫的驗收測試，三條的第一行都是「一支都不在場就 skip」。
於是**部分遺失會紅，全部遺失反而安靜**。

skip 的條件已改成 **`work/` 這個目錄不存在**，不是「一支都沒有」。
三種狀態各驗過一次：不存在 → skip；存在但空 → **紅，訊息直接指向垃圾桶
與還原指令**；只剩 1 支 → 紅。

> **我今天早上寫的驗收測試，在它第一次真的該說話的時候沉默了。**
> 而它沉默的原因，正是我在同一份文件裡寫「三條分開寫是刻意的」時
> 沒想到的第三種狀態。

## 今天的形狀

前兩次工作日誌的形狀都是「**拿來比／拿來測的那個東西，本身沒有被檢查過**」。
今天的形狀不一樣：

> **被檢查的東西是對的，但那不是要交付的東西。**

七天的量測沒有一次出錯（CV 0.34、WindowDiff、溯源通過率都是真的），
它們量的是 `chunks.jsonl`——而 `chunks.jsonl` 不是 v1 的交付物。
**指標本身沒問題，是指標指的對象選錯了。**

而選錯的原因是：**從來沒有人問過「v1 交付的是什麼」。**
