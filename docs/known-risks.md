# 已知風險與未驗證的假設

SDD §9 列的是設計時就看得到的風險。這裡記的是**實作過程中發現、且尚未被
測試涵蓋**的東西。每一則都要說明「什麼時候會爆」與「何時能驗證」。

寫下來的目的不是免責，是不讓它們消失在腦袋裡。

---

## R1：合成人臉 ≠ 真實人臉（Phase 1 必須實測）

**現況**：合成講者幀已實測可被 `haarcascade_frontalface_alt2` 偵測（面積佔比
0.131，投影片幀全為 0），見 `docs/decisions.md` D6，測試在
`test_unit_synth.py::test_speaker_frames_are_detectable_as_faces`。

**風險**：這只證明**合成素材不會產生假失敗**，不證明分類器在真實影片上有效。
真實講經影片的講者可能側臉、低頭看稿、戴眼鏡、光線不足、被字幕遮擋。

**何時會爆**：真實影片的 speaker/slide 分類 accuracy 掉到 0.95 以下。

**2026-08-04 實測結果：已確認失效，見 R8。** 真實素材的人臉面積佔比為
0.005–0.026，遠低於 0.04 門檻（合成素材是 0.131）；而且全螢幕投影片中的
人像（胚胎圖）也會被偵測成「人臉」0.024，與講者畫面完全重疊。
**人臉大小在這份素材上不具區辨力。** 詳見 R8。

---

## R2：真實影片黃金集尚未標註（阻擋 4 項門檻）

**現況**：SDD §5.1（B）要求手工標註 3 支真實影片（含目標 playlist 至少 2 支），
成本約每支 30 分鐘。尚未開始。

**被阻擋的門檻**（§5.2）：

| 門檻 | 值 |
|---|---|
| 換頁偵測 boundary F1（真實） | ≥ 0.75 |
| speaker/slide 分類 accuracy | ≥ 0.95 |
| 術語校正 precision | ≥ 0.90 |
| 對齊邊界誤差中位數 | ≤ 5 秒 |

對應的 e2e 測試目前是 `pytest.skip`，**不是**假通過——skip 會出現在報告裡。

**風險**：合成影片全綠但真實影片崩掉。合成素材是自己畫的，難免帶著自己的
偏見（版型規整、對比清晰、無壓縮劣化、無鏡頭運動）。

**何時驗證**：Phase 1 早期。應在寫完 S1b **之前**先標註，否則會不自覺地
朝著合成素材過擬合。

---

## R3：`§5.5 #3` 無法自動檢查

**規定**：「不得只用逐字稿讓 LLM 猜投影片邊界或投影片內容。」

**現況**：`test_unit_conventions.py` 能機械式檢查 §5.5 的 #6、#7、#9、#10、
#11、#12、#13。#1（固定間隔取樣冒充偵測）、#2（跳過分類）、#3（用逐字稿猜）
**檢查不到**——它們是「演算法實際上做了什麼」的問題，不是程式碼形狀的問題。

**緩解**：
- #1、#2 間接被對抗樣本擋住：固定間隔取樣在 A4（雷射筆）與 A5（內嵌影片）
  會爆量 FP，跳過分類在 A3（純講者）會偵測出投影片。F1 門檻自然會抓到。
- #3 只能靠 code review 與 §5.6 的人工抽檢。

---

## R4：PaddleOCR 對直排文字的準確度未實測 ✅ **已解除（v0.3 移除本地 OCR）**

**來源**：SDD §9 已列。這裡補記實作面的影響。

**2026-08-04 實測：合成素材上已解除。** PaddleOCR 對六種版型信心 0.98–1.00，
直排經文六行全對（見 docs/decisions.md D10）。真實素材 47 張平均信心 0.913、
僅 2 張無文字。

**簡繁問題已隨本地 OCR 一併消失（v0.3）。** PaddleOCR 的 `ch` 模型輸出
簡體（實測「万批交城」「冷顺型辉」），與繁體素材不符；v0.3 移除本地 OCR
後，投影片文字改由 S4 的 VLM 讀出，模型會依畫面輸出繁體，沒有字集轉換
的問題。jieba 詞典是簡體的問題（D12）同樣消失。

**額外風險**：合成的直排是「逐字往下堆」，字距均勻、無標點壓縮。真實的直排
排版（尤其古籍風格）會有標點旋轉、行間注、異體字。

**何時驗證**：Phase 1，S2 實作時。SDD §9 已定緩解方向：詞庫只求高頻術語覆蓋，
精確理解交給 S4 的 VLM。

---

## R5：OpenCV 5.0 已移除 CascadeClassifier

**現況**：已鎖 `opencv-python-headless>=4.10,<5`，見 `docs/decisions.md` D4。

**風險**：4.x 不會永遠有 wheel。屆時要改用 `FaceDetectorYN`（需另外下載並
版控 ONNX 模型權重）。

**觸發條件**：pip 找不到 4.x 的 wheel，或 PaddleOCR 要求 5.x。

---

## R6：`pipe-gpu` 環境尚未建立

**2026-08-04：已建立並驗證。**

1. **PyTorch 從官方 index 裝**（SDD §8 雷 1）：`torch 2.11.0+cu128`，
   在 sm_120（capability 12,0）上實跑 matmul 通過。
2. **ffmpeg 用系統 apt 版本**（雷 2）：`/usr/bin/ffmpeg` 6.1.1。
3. PaddleOCR 需額外關閉 oneDNN，且必須用 CPU 版 paddle 3.x——
   PyPI 的 `paddlepaddle-gpu` 只到 2.6.2，與 paddlex 3.7 不相容。
   見 docs/decisions.md D10。

**VRAM 約束**：Whisper large-v3 與 Qwen3-VL 8B **不可同時常駐**於 16GB，
必須分階段載入／卸載。

---

## R7：具名實體檢查不涵蓋人名

**現況**：刻意的設計限制，理由見 `docs/decisions.md` D3。

**風險**：SDD §5.4 明文要求「人名、書名、數字、年代」都要檢查。目前人名這一
項是**缺的**，靠 Phase 2 的系列術語詞庫補。在那之前，S4 若編造人名不會被抓到。

**何時補**：Phase 2，S2b 詞庫可用之後。屆時把詞庫中的專有名詞當作實體白名單，
block 中出現的、不在詞庫也不在來源的候選專名一律標 `unverified`。

---

## R8：真實素材的畫面結構與 SDD §1.3 的描述不符 ✅ **已解決（SDD v0.2）**

> ### 結論摘要
>
> **已於 2026-08-04 修訂 SDD v0.2 解決。** 三項處置：
>
> 1. **§1.3 降級**為「單支影片的 profile 範例」，不再是設計前提；
>    新增 **S-1 素材勘查階段**（§4.0），每個新系列開跑前強制執行，
>    畫面模式與預期不符即中止並報告
> 2. **§4.3 步驟 2** 的分類由人臉大小改為**背景距離 + Otsu**
>    （分離度 5.07x，對鏡頭運動有 8–10x 餘裕，見 `decisions.md` D14）
> 3. **§4.3 步驟 5** 的 keyframe 由「取段末幀」改為「取段內 ink 量最大的
>    幀，排除兩端」（D15）
>
> **下方原文中「存在第三種模式：半透明疊加」的判斷是錯的。**
> 逐秒檢視 2519 幀後確認那是 1 秒交叉淡化轉場（全片 10 幀，0.40%），
> 不是持續的呈現模式，**IR 不需要第三種 mode**。
> 完整量測見 `experiments/r8_overlay/REPORT.md`。
>
> **原文保留**——它記錄了一次規格錯誤是怎麼被發現的：先看到產品輸出不對
> （chunk 是「里明品带务务念」），再往回追到 §1.3 的假設。而過程中我對
> 「疊加」的判斷本身也錯了兩次。這個追查路徑比結論更值得留存。

---

### 以下為 2026-08-04 當時的原始記錄（判斷有誤，見上方摘要）

> **這是設計層級的問題，需要決定，不是我單方面可以改的。**
> SDD §5.5 開頭：「若實作者（含 AI coding agent）認為某項規定阻礙進度，
> 正確做法是**停下來提出討論**，而不是繞過。」

### SDD §1.3 怎麼說

| 特性 | SDD §1.3 的描述 | 對設計的影響（SDD 原文） |
|---|---|---|
| 畫面切換 | **硬切**：全螢幕講者 ↔ 全螢幕投影片 | 不需要 ROI 裁切；需要 speaker/slide 二分類 |
| 純講者時段 | 大量存在（無投影片） | IR 必須能表達 `mode: speaker_only` |

§4.3 步驟 2 據此設計：「偵測滿版人臉 → 標記為 speaker 幀（本素材為硬切，
無 PiP，故此步足夠）」。

### 實際素材（zIglvjoU9vo，42:00，游朱義《古典醫學之人體設計系列-1》）

以「與攝影棚常態的畫面距離」對 2519 幀分群，分布明確雙峰：

| 模式 | 幀數 | 佔比 |
|---|---|---|
| 固定攝影棚機位（講者坐在桌前，背景固定） | 2042 | **81.1%** |
| 明顯不同的畫面（全螢幕投影片／片頭／片尾） | 468 | **18.6%** |

**三個與 §1.3 不符之處：**

1. **不是「全螢幕講者 ↔ 全螢幕投影片」的硬切。** 講者**始終在畫面中**
   （固定攝影棚廣角機位），只有約 19% 的時間切到全螢幕投影片。

2. **存在第三種模式：半透明疊加。** 投影片內容（經文、標題）以半透明方式
   **疊在講者畫面上**（例如 `03_slides/slide_017.png`：太上老君內觀經文字
   疊在攝影棚畫面上）。§1.3 完全沒有提到這種模式。這也代表把攝影棚畫面
   一律歸為 `speaker_only` 會**丟掉疊加的投影片內容**。

3. **人臉不是「滿版」。** 實測面積佔比 **0.005–0.026**，門檻是 0.04；
   合成素材的講者幀是 0.131（差 5 倍）。

### 為什麼不能只把門檻調低

t=1600s 是一張**胚胎發育圖的全螢幕投影片**，Haar 偵測器在上面找到「人臉」，
面積佔比 0.0244——與攝影棚畫面的 0.023–0.026 完全重疊。把門檻調到 0.02
會讓這類投影片被誤判為講者。**人臉大小在這份素材上不具區辨力。**

### 目前的實際後果

- 2519 幀中只有 18 幀被判為 speaker（應約 2042 幀）
- 換頁偵測因此在攝影棚畫面內部運作，對**講者的動作**反應
- 47 個段落中有 **18 個短於 10 秒**（中位停留 15s，§5.1 預期 30–120s）

### 可能的方向（未實作，但已實測給數據）

**方向 1：改用「與背景常態的距離」做二分類**，取代人臉大小。

作法：取全片的中位幀作為「攝影棚常態」，算每幀與它的距離，用 Otsu 自動
找雙峰谷底（與 ink 遮罩相同的手法，尺度自適應，無需手調門檻）。

實測結果（zIglvjoU9vo）：

| 指標 | 人臉法（現況） | 背景距離法 |
|---|---|---|
| 判為講者的幀 | 18（0.7%） | 2053（81.5%） |
| 全螢幕投影片段落 | 47 | **19** |
| 短於 10 秒的段落 | 18 | **7** |
| 中位停留 | 15s | 12s |

Otsu 自動門檻落在 0.1665，分出 466 幀（18.5%）為全螢幕投影片——與獨立
量測的 18.6% 吻合。**這個方向可行且不需手調參數。**

**方向 2：新增第三種 mode 表達「疊加」。**

實測發現**這個方向沒那麼容易**：疊加期間的 ink 量與一般攝影棚畫面幾乎
沒有差異（24851–26955，僅差 1.1 倍），因為**攝影棚背景本身就滿是文字**
（背板上的「追求法喜的身體健康」等字樣）。ink 量已飽和，無法區分。

要偵測疊加需要別的訊號——例如與「無疊加的攝影棚基準幀」相減後的殘差，
或直接對攝影棚幀也跑 OCR 並比對文字量變化。兩者都是新的機制。

**方向 3：保持現狀**，接受這支影片的偵測品質，靠 §5.6 的人工抽檢把關。

方向 1 與 2 都需要修改 SDD §1.3 與 §4.3，因此**沒有實作**。
方向 1 的實驗程式碼未進版控（一次性測量），數據如上表。

### 對產品的具體影響（2026-08-04 實測）

把真實素材的 S3 輸出接上 S6（Understanding 手工建立，內容取自真實 OCR），
整條鏈**技術上跑得通**——38 個 chunk、溯源 0% 未通過、§5.3 全部不變量
通過。但產出的 chunk 內容是：

```
"text": "里明品带务务念"        ← 攝影棚背板的裝飾字，非投影片內容
"text": "探索人體追求康"        ← 同上
```

因為被當成「投影片」的幀其實是攝影棚畫面，OCR 讀到的是背板上的裝飾文字。
**管線的每一個機械檢查都通過，產品卻是廢的**——這正是 §5.6 人工抽檢
存在的理由，也說明 R8 不是效能問題而是正確性問題。

### 對驗收的影響

§5.2 的「換頁偵測 boundary F1（真實影片）≥ 0.75」在這份素材上目前**無法
評估**——沒有黃金集標註（R2），而且在釐清「什麼算一次換頁」（全螢幕切換？
疊加內容變更？）之前，標註本身沒有明確定義。

---

## R9：**§5.4 溯源檢查失去了獨立的比對基準**（v0.3 的主要代價）

**這是移除本地 OCR 最重要的後果，優先於本文件其他所有風險。**

§5.4 是 SDD 明文的「防止 LLM 編造內容的**唯一**有效機制」。它的作法是把
`content_block` 的內容與 `provenance` 指向的來源比對相似度。

| | 來源 | 獨立性 |
|---|---|---|
| v0.2 | PaddleOCR 讀出的投影片文字 | **獨立**——OCR 與 LLM 是兩個系統，LLM 編造的內容不會出現在 OCR 結果中 |
| v0.3 | **S4 的 VLM 自己讀出的** `slide_text` | **不獨立**——同一個模型既產生來源、也產生待驗證的內容 |

**具體的漏洞**：模型若系統性誤讀投影片（例如把「識蘊」讀成「時運」），
它產生的 `slide_text` 與 `content_blocks` 會**一致地錯**，溯源檢查全部通過。
獨立 OCR 版本則會在兩者不一致時把它標出來。

**已實作的部分緩解**：`RESPONSE_SCHEMA` 的欄位順序是
`is_slide → slide_text → content_blocks`。structured output 是逐欄生成的，
先產生的欄位不能因後面的內容而回頭修改，所以模型必須**先逐字轉錄、再詮釋**。
prompt 也明寫「先抄，再詮釋…不要在這裡改寫、摘要或補充」。

這擋得住「詮釋時越說越多」（最常見的幻覺形式），擋不住「從一開始就讀錯」。

**還能怎麼補**（未實作，需要決定）：

1. **獨立的二次 OCR**——只對 `is_slide=true` 的候選幀跑，數量遠少於 v0.2
   （實測 52 個區段中預期約 19 個是投影片）。這會把 PaddleOCR 加回來，
   連同 D10 記錄的那些坑
2. **交叉檢查逐字稿**——講者通常會唸出投影片上的關鍵詞。若 `slide_text`
   中的術語完全不出現在該時段逐字稿中，標為可疑
3. **接受它**，靠 §5.6 的人工抽檢把關。§5.4 的自動閘門降級為「抓越說越多」，
   不再宣稱能抓「讀錯」

**目前採用第 3 項**（因為 v0.3 的目的就是移除本地 OCR）。**這代表 §5.4
的保護強度實質下降了**，不是等價替換。

---

## R10：S3 的語意邊界吸附已移除

**原設計**（SDD §4.6 步驟 2–3）：取邊界前後 ±20 秒的句子，計算其 embedding
與前後兩張投影片 OCR 文字的相似度，把邊界吸附到語意轉折點。

**為什麼移除**：吸附需要**投影片文字**。v0.3 移除本地 OCR 後，投影片文字要
到 S4 才有；而 §4.6 明文禁止 S3 呼叫 LLM（避免與 S4 循環依賴）。兩個約束
一起使得吸附在 S3 無法進行。

**實測上損失有限**：D11 的量測顯示真實素材的相鄰投影片語意相似度達 0.873，
超過 0.85 的守門門檻，吸附本來就不會作用。合成素材 A1 上吸附有效，但那是
版型差異刻意做大的結果。

**被影響的門檻**：§5.2 的「對齊邊界誤差中位數 ≤ 5 秒」。黃金集尚未標註
（R2），所以目前沒有測試因此失敗——**這不代表沒有損失，只代表還沒量到**。

**若要加回來**（需要決定）：唯一不製造循環依賴的作法是把吸附移到 S4
**之後**，用 S4 產出的 `slide_text` 做第二次對齊。但 S4 的理解是在未吸附的
邊界上算出來的，改了邊界後理解未必仍然成立。

**相依效果**：`sentence-transformers` 與 BGE-M3 已從相依中移除，S3 不再需要
GPU。

---

## R11（部分解除，見 D25／R20）：文言文 ASR 的 initial_prompt 緩解已移除

**原設計**（SDD §9）：把系列術語詞庫餵進 Whisper 的 `initial_prompt`，
提高文言文與專有術語的辨識率。

**為什麼移除**：詞庫來自本地 OCR（S2b），已隨 v0.3 一併移除。

**現況**：錯字改由 S4 對照投影片畫面修正（`corrections`）。差別在於**時機**
——原本是「讓 ASR 一開始就少錯」，現在是「事後修」。事後修改不了 ASR 完全
聽錯而寫出通順句子的情形。

**風險有限的原因**：目標素材 `zIglvjoU9vo` 有 **zh-Hant 手動字幕**，
依 §4.2 的優先序根本不會跑 Whisper。此風險只在無字幕的影片上才浮現，
而那類影片尚未實測。

**若要加回來**：不需要恢復整條 OCR 鏈——可以用**已處理過的同系列影片**的
S4 輸出（`Understanding.terms`）建立系列術語表，餵給後續影片的 Whisper。
這保留了 §2.1 原則二（本地扛量、雲端扛判斷），也不引入新相依。

---

## R12：正向溯源門檻只校準過一種 content_type ⚠️ **已校準，但結論是「指標不適用」**

**背景**：`docs/decisions.md` D1 用「忠實改寫 vs 對抗性幻覺」實測定出
`min_similarity = 0.25`。但那批素材全是**書面 vs 書面**（白話解說 vs
投影片文字）。

**首跑實測**（zIglvjoU9vo，79 個 content_block，來源非空者）：

| 型別 | n | 中位 | 與來源的關係 | 現行門檻擋掉 |
|---|---|---|---|---|
| 經文原文 | 13 | **1.000** | 逐字引用 | 0% |
| 白話解說 | 13 | **0.385** | 改寫書面文字 | 23% |
| 口頭延伸 | 31 | **0.250** | **壓縮口語** | **48%** |
| 圖表描述 | 13 | **0.228** | **描述視覺結構** | **54%** |

白話解說的 0.385 正好落在 D1 實測的 0.364–0.417 區間——**D1 的校準是對的，
但只對這一種型別**。另外兩種從沒量過。

**為什麼另外兩種結構性偏低**：

- **口頭延伸**：來源是口語逐字稿，充滿重複與贅詞（「房子是給什麼住的
  給人住的」），而 block 是壓縮後的書面語（「胎兒進入子宮後會評估環境
  是否適合居住」）。**詞彙落差是結構性的，不是不忠實。**
- **圖表描述**：block 描述的是**版面結構**（「左側標註時間軸，右側標註
  功能漸齊備」），而來源 `slide_text` 是**內容詞**。兩者本來就不共用詞彙。

**規格的不對稱**：SDD §5.4 對**反向**檢查已明訂「檢查須依 type 分別設定
範圍」（`max_copy_ratio`），正向檢查卻只有單一 `min_similarity`。

### 校準結果（2026-08-04，`experiments/r12_calibration/REPORT.md`）

比照 D1 建了逐型別的「忠實 vs 幻覺」對照組。**結論是這個指標不適用**：

| 型別 | 分離倍數 | 判定 |
|---|---|---|
| 經文原文 | **7.50x** | 可用 |
| 白話解說 | 1.14x | 不可用 |
| 口頭延伸 | **0.54x** | 不可用（分布**反轉**） |
| 圖表描述 | 0.00x | 不可用 |

連 D1 校準過的白話解說都只有 1.14x——因為這次的幻覺樣本是**部分編造嫁接
在真引文上**（「一月為胞，精血凝也，此說與《黃帝內經》…互為表裡」），
真的那半把分數撐起來。D1 的幻覺是整段虛構，太好擋。

四個失效原因，全部是**結構性**的：口語→書面的語域落差、描述版面 vs 內容詞、
**跨語言時忠實與幻覺都是 0.000**、以及來源比 block 短時的數學上限。

### 已做的處置

1. **擴充具名實體的單位清單**——原本漏掉週／天／種／成／公分等，
   「四種」「七成」「第五週」「二十一天」全部抓不到。補上後幻覺攔截
   從 5/12 升到 8/12，誤報零增加。**具名實體檢查才是能推廣的那道。**
2. 逐型別的正向門檻，但**不假裝它有鑑別力**——後三型只擋「與來源完全
   無關」的退化情形。
3. 真實資料上未通過率從 43.0% 降到 **7.5%**。

### 仍未解決

**純質性編造抓不到。** 4/12 的幻覺沒有任何具名實體，只有編造的陳述
（「建議孕婦保持愉悅、閱讀或聽經」）。containment 與具名實體檢查都無效。

**§5.4 的 5% 門檻在目前的檢查下不可達**——結構性誤殺率約 6%。剩下的 5 筆
未通過中，1 筆是真問題、4 筆是誤殺；再調門檻會把唯一抓到的真問題也放行。
這是規格層級的取捨（改進檢查 vs 承認閘門只能當粗篩），不是參數問題。

---

## R13：VLM 的術語校正超出授權範圍（v0.3 首跑實測）— **已處置，仍有缺口**

> **2026-08-04 結論摘要**（詳見 `experiments/r13_corrections/REPORT.md`）
>
> 校準時發現了比原記載更根本的問題：**prompt 寫的授權條件本身是錯的。**
> 「正確寫法出現在這張投影片上」在實測 9 筆中**只有 1 筆**成立，
> 而那 1 筆恰好是插入型違規。若把它實作成程式規則，會擋掉 4 筆合規校正、
> 放行唯一危險的那筆——**鑑別力是負的**。原因是 ASR 的同音錯字出現在
> 講者口述中，不在投影片上；5/9 的校正甚至來自 `is_slide=False` 的區段。
>
> 已做的處置：
>
> 1. **改寫 prompt 的授權定義**為「發音沒變、只是字寫錯」，並補上五類
>    負面示例（插入／事實修正／語意改寫／潤稿／格式化），各附實測案例。
> 2. **程式端三道兜底**（`weft.validation.corrections`）：插入 >+2 字、
>    拼音相似度 <0.35、casefold 後相同。
>
> 效果：首跑那 9 筆的 precision **0.56 → 1.00**，對照組誤殺授權 **0/15**。
>
> **仍有缺口——事實修正擋不到**（`唐朝`→`宋朝` 拼音 0.500、
> `六祖惠能`→`六祖慧能` 拼音 1.000，與授權樣本完全重疊）。
> 這是量測後判定程式端**做不到**，不是還沒做：「同音但改變事實」與
> 「同音錯字校正」在字串層面沒有可分的特徵。剩下的防線是 prompt 的負面
> 示例與 **§5.6 人工抽檢**；傷害可逆是因為 `text_raw` 永不覆寫（§4.5 約束 3）。
>
> **拼音門檻的餘裕只有 1.60x，低於 D1／R12 沿用的 2x 判準**——採用的理由
> 與代價寫在 REPORT §4。授權側只有 5 筆是實測樣本，n 不足，累積更多真實
> 校正後應重新校準。

以下為 v0.3 首跑當時的原始記錄，保留。



**prompt 明訂**：「只改**有把握**的：正確寫法出現在這張投影片上，且與錯字
同音或近音」「**寧可漏改，不可亂改**」。

**首跑的 9 筆校正中，約 4 筆違反此規則**：

| 校正 | 問題 |
|---|---|
| `啟示經 → 創世記` | **事實修正**，不是同音錯字。創世記未出現在投影片上 |
| `投胎轉世 → 十個月懷胎` | **語意改寫**，兩者不同音也不同義 |
| `陽神為三魂 → 陽神為三魂，動而生也` | **插入**講者沒說過的字 |
| `沒sense → 沒Sense` | 大小寫，不是術語校正 |

合規的 5 筆品質良好（`意地論 → 瑜伽師地論`、`憍梵钵提 → 憍梵波提`、
`家家當 → 家當` 等）。

**依 §5.2 的定義，precision ≈ 5/9 = 0.56，低於 0.90 的門檻。**

**危險性**：`陽神為三魂 → 陽神為三魂，動而生也` 這類**插入**特別危險——
它把模型的補全寫進 `text_corrected`，而 §5.3 不變量 10 只檢查 `from` 出現
在原文中（確實有），檢查不到 `to` 是否加了原文沒有的內容。

**可能的修正方向**（未實作）：

1. **prompt 加負面示例**——明列「不要做事實修正」「不要補全省略的內容」
2. **程式端加約束**：拒絕 `len(to) > len(from) * 1.5` 的校正（插入）；
   拒絕拼音相似度過低的校正（語意改寫）。後者需要 pypinyin，但只用於
   **驗證**而非產生，比 v0.2 的整條詞庫鏈輕得多
3. 兩者都做——prompt 負責減少，程式負責兜底

---

## R15：本地 VLM 的可行性——轉錄夠用，分類不夠（2026-08-04 實測）

**動機**：瓶頸不是模型能力而是 **20 RPD**。一支 42 分鐘影片 49 個區段＝
三天配額；本地跑完只要 2 分鐘且無上限。

**實測**（RTX 5070 Ti 16 GB / ollama，詳見 `experiments/r14_local_vlm/REPORT.md`）：

| | gemma3:4b | gemma3:12b | qwen2.5vl:7b |
|---|---|---|---|
| is_slide 準確率 | 48.9% | 65.3% | 69.4% |
| 誤報（佈景當教材） | 24 | 15 | 15 |
| CER（簡單投影片） | 6.7–35% | 0–7.7% | 0–6.7% |
| CER（內觀經，密集文言＋直排） | 18.1% | 20.2% | 56.9% |
| 自我一致性（同圖 10 次） | 22.5% | 5.2% | 18.0% |

**擋路的是 `is_slide` 不是 OCR。** 講者背後那面牆有大量書法標語，
本地模型照單全收抄進 `slide_text`——SYSTEM_PROMPT 明文警告過，三個都沒接住。

三種模型的失敗方式不同，值得記著：

- **gemma3:12b** 認錯字（`天地媾精`→`天上諴精`、`降真靈也`→`除真靈也`），
  會直接汙染溯源基準。
- **qwen2.5vl:7b** 字全對但**讀錯欄位順序**——左欄讀完才讀右欄，
  破壞了 `一月為胞`↔`精血凝也` 的配對。**這種失敗看起來很像成功，
  沒有任何自動檢查抓得到。**

**未解**：`is_slide` 不一定要 VLM 做。攝影棚鏡頭全片高度重複，
是 CV 的強項。若這條路通，本地方案的缺口就補上了，並順帶解掉 R9
（不同模型當溯源裁判不再需要額外額度）。**這一步尚未量測。**

**注意**：報告中 Gemini 的所有數字都帶著 D20 的圖片錯位，
它的真實能力只會比表上更好。配額重置後須用同一份標註重測。

---

## R16：Whisper 路徑輸出簡體，且術語錯誤是人工字幕的數量級之上（2026-08-06 實測）

**這條路 v0.3 寫好至今從未執行過**，測試裡零提及。R17 是第一次跑。

實測（`experiments/r17_whisper/REPORT.md`）：程式能跑，42 分鐘影片 3.7 分鐘
產出 999 個 cue。但輸出不能直接接進管線。

| | 人工字幕 | Whisper |
|---|---|---|
| 簡體字比例 | 0.00% | **9.43%** |
| 字元錯誤率（轉繁後） | — | **12.4%** |
| 字元錯誤率（未轉繁） | — | **39.5%** |

**問題 1：繁簡。** 27 個百分點的錯誤率純粹是字集問題。§5.4 用 bigram
containment 比對 block 與 `slide_text`，而 `slide_text` 是 VLM 從**繁體**
投影片讀出來的——簡體逐字稿與繁體投影片的重疊會結構性崩潰，與 R12 量到
「跨語言時 containment 必為 0」是同一個機制。R13 的校正也會失效：
§5.3 不變量 10 要求 `from` 一字不差出現在 `transcript_raw`。
**D12 記的簡體問題沒有消失，只是換了入口。**

**問題 2：術語。** 轉繁後 19 個受檢術語仍錯 11 個，且集中在經文與佛學術語
（`形兆胚`→`形造胚`、`五行分藏`→`五行分障`、`六律定腑`→`六律定府`、
`七精開竅`→`七經`）；一般口語幾乎全對。人工字幕上管線只做了 7 筆校正，
Whisper 光這 11 個術語就出現 31 次。

**R13 補不上**：它只校準了 precision，**recall 從未量測**；
prompt 被設計成保守（「寧可漏改，不可亂改」）；而且 `七精開竅`→`七經`
這種吞字的拼音相似度很低，會被 R13 的程式端**主動擋掉**。
**R13 是為「人工字幕的零星錯字」設計的，不是為「ASR 的系統性術語錯誤」。**

**意外發現**：人工字幕從 70.7s 才開始，Whisper 從 0.21s 就有。
那 71 秒是節目開場旁白（18 cue / 159 字），首跑時 seg_000–006「逐字稿為空」
就是這個原因——我當時當成「片頭沒有內容」處理，**其實是有的**。
這讓策略 3（兩份都留）的價值高於 SDD 原本的設想：不只是交叉檢查，
**兩邊的涵蓋範圍本來就不一樣**。

---

## R17：策略 3（雙重驗證）三個設計問題未解，且在現有素材上測不了

`zIglvjoU9vo` 的 `has_auto_caption = False`——沒有自動字幕，無從測起。

未解且**皆未量測**：

1. **兩份逐字稿如何對齊**：分段與時間戳都不同，要先做序列對齊。
2. **不一致 ≠ 錯在哪裡**：兩套 ASR 可能在同一個罕見術語上一致地錯，
   也可能在贅詞標點上大量無意義地不一致。
   「不一致處就是術語錯誤高發區」目前只是假設。
3. **獨立性未量**：YouTube 自動字幕（Google）與 Whisper（OpenAI）比 R9 的
   處境好（R9 是同一個模型驗自己），但沒有數據。

---

## R18：SDD 策略 3 的前提在這個語料上不成立（2026-08-06 實查）

**SDD §4.2 策略 3**：「有自動字幕且也跑了 Whisper → 兩者都存，
自動字幕存為 `alt` 供交叉檢查。」

`yt-dlp --list-subs` 實查（`experiments/r19_dual_asr/REPORT.md` §1）：

- `zIglvjoU9vo`：唯一非翻譯的軌 `zh-Hant` **就是那份人工字幕**。
  所有 `automatic_captions` 都是「X **from** Chinese (Traditional)」，
  即**從人工字幕機器翻譯**出來的，不是獨立語音辨識。
- `q3iM-jlbQBA`、`XJXbSaenwnY`、`mMlQEpu6GRI`（同講者）：
  `has no automatic captions` / `has no subtitles`——**什麼都沒有**。

這個語料只有兩種影片：有人工字幕的（YouTube 因此不跑 ASR），
與什麼都沒有的。**「自動字幕 + Whisper」無從實施，SDD §4.2 策略 3 須改寫。**

**替代方案已量測且有效**（R19）：改用第二個本地 ASR。
以 Whisper medium 當 ASR #2，術語層級的交叉檢查 **recall 93%**、
不一致率 39%，兩者「一致地錯」只有 2/30。

**關鍵設計結論**：訊號**必須綁在術語上**。原始文字層級的不一致率是 **90%**
（贅詞、標點、分段本來就會大量不一致），拿它當訊號等於每句都要人看。

**主要限制**：術語清單在無字幕影片上只能來自 `slide_text`，
實測只涵蓋 72 個術語中的 **38 個（53%）**——投影片沒提到的術語
（`投胎轉世`、`中陰身`、`五蘊`…）交叉檢查沒有依據。**尚未有解。**

---

## R19：§5.2 有三項門檻懸空——測試全綠但什麼都沒在管（2026-08-07 盤點）

準備解除 R2（黃金集）前先盤點「要標什麼」，結果發現
**10 項門檻只有 3 項真的在拿實際輸出驗收**
（`experiments/r24_threshold_audit/REPORT.md`）。

| 狀態 | 門檻 |
|---|---|
| ✅ 真的在跑 | 合成 boundary F1、逐條動畫合併、溯源通過率／unverified 比例 |
| ⏸ 缺標註 | 真實 boundary F1 |
| ❌ 機制已不存在 | speaker/slide 分類 accuracy |
| ❌ 從來沒有驗收測試 | 術語校正 precision／recall、對齊誤差 |

**最後三項的測試 body 只有一行 `pytest.skip`**——不是「有標註就會跑」，
是根本還沒寫。而 `test_unit_metrics.py` 裡有
`assert T.TERM_CORRECTION_PRECISION == 0.90` 這種**常數值斷言**，
它會綠燈、看起來像門檻有在管，但只證明了常數沒被改過。

**§5.5 #7 防的是「有人偷偷調門檻」，沒有規定防「門檻根本沒接上」。**
已加機械護欄 `test_every_acceptance_threshold_is_actually_enforced`。

### 仍然懸空的兩項（列在 `DANGLING_THRESHOLDS`）

**`TERM_CORRECTION_RECALL`**：值為 `None`，語意是「必須量測並寫入報告，
但不 assert」。**實際上從來沒有任何地方量測它並寫入報告。**
R13 §「未量」明白記過「recall 從未量測」，至今仍是。
要解除需要黃金集標註「逐字稿中所有應該被改的術語錯誤」。

**`ALIGNMENT_MEDIAN_ERROR_SEC`**：v0.3 移除語意吸附（R10）後，
S3 只做粗切、`boundary_shift_sec` 恆為 0——**沒有「吸附」可言，
這個門檻量的是什麼需要重新定義**。在定義清楚之前不接上，
接上一個定義不明的東西比空著更糟。

### 已處置的一項

**`FRAME_CLASS_ACCURACY`** 測的 `frame_class` 在 v0.3 用 D16 移除 CV
分類後全部是 `slide`（實測 2519/2519）。分類這件事沒有消失，
只是搬到 S4a 的 `is_slide`（§4.7a）——門檻已改綁到那裡，見 D30。

---

## R20：投影片分類門檻紅燈 0.818——但錯誤 100% 落在定義不清的地方

B1 讓 §5.2 的分類門檻（`SLIDE_CLASSIFICATION_ACCURACY ≥ 0.95`）
第一次真的跑起來，結果紅燈。**這是誠實的紅燈，不調門檻（§5.5 #7）。**

拆開看：

| 類別 | 準確率 |
|---|---|
| **核心**：全螢幕投影片 vs 純攝影棚講者鏡頭 | **33/33 = 100%** |
| **歧義類**：片頭片尾動畫、講者活鏡頭＋疊加文字 | **7/15 = 46.7%** |

三支影片 8 個錯誤，**沒有一個落在核心類**；而且兩支錯的是同一批
（片頭 logo、片頭標題卡、主講人簡介 ×2）。46.7% 接近擲骰——
**那不是模型不會，是題目沒有答案**：主講人簡介同時滿足現行定義的兩邊
（有活鏡頭 → 不是；有準備好的文字 → 是），定義沒說哪邊優先。

**處置**：已寫成 `docs/proposals/slide-definition.md`（收緊為兩條件）。
**刻意沒有自己改標註**——「改 ground truth 讓紅燈變綠」正是 §5.5 存在
要防的事，即使理由充分。等決定。

**在採納之前這個門檻會一直紅。** 那是對的：它反映的是一個真實的
規格缺口，不是測試壞了。

---

## R21：分類判準改到 v5 後仍未達 0.95——改 prompt 措辭已到頂

R20 的定義缺口已由使用者決定（主講人簡介不算投影片）並落地，
但門檻**仍然紅著**。完整量測見 `experiments/r26_slide_definition/`。

同一個模型（`ollama:gemma4:12b`）、同一批圖、temperature 0，
五個 prompt 版本：

| 版本 | 調校集（3 支 58 張） | 保留集（1 支 22 張） |
|---|---|---|
| v1 | 0.818 | — |
| v2 | 0.636 | — |
| v3 | 1.000 | 0.909 |
| v4 | 0.861 | 0.952 |
| v5 | 0.885 | 0.909 |

**光改措辭就在 0.636 與 1.000 之間跳。** 到了 v3 之後，分數的變動反映的
已經是模型對指令措辭的敏感度，不是定義還有多不清楚——
定義的模糊在 v1 那一版是主因（錯誤 100% 落在歧義類），現在不是了。

**v3 的 1.000 是假的**：那三支影片全是攝影棚定鏡，20 張正例沒有一張
看得到講者，所以「有講者 → 不是」在上面 20/20 成立。保留集是講堂實景，
講者站在投影幕前，v3 的錯誤恰好就是那兩張分割畫面。
**這是本次最值錢的發現，已制度化為 `GoldenAnnotation.holdout` 與測試斷言。**

### 還沒用的一張牌：跨集重現訊號

v4 的判準「這些字換一集還會不會一樣」邏輯上涵蓋全部負例，
但**模型一次只看得到一張圖**，那個資訊不在它的輸入裡。
交給 CV 就成立——對每張代表幀算它與其他集所有代表幀的最小灰階 MAE：

| | 跨集最小 MAE |
|---|---|
| 是投影片（n=26） | 最小 **7.57** |
| 不是（n=54） | 最小 0.44、中位 3.61 |

門檻 6.0：抓到包裝 29/54、**誤殺投影片 0/26**。
接在 v5 前面當前濾，合計 0.900 → **0.925**。

**已做進 `src/`**（2026-08-08，使用者決定接受這個跨集依賴）。
`stages/recurring.py`，S4a 的第 0 趟，設定為 `s4a.cross_episode_filter`。

實跑四支：**剔除 29 張，誤殺真投影片 0 張**，合計 0.900 → **0.925**
（與離線量測完全一致）。被它剔掉的不必再花一次分類呼叫。

參考影片少於 2 支時**明說並跳過**，不靜默降級（§5.5 #6 的精神）——
這一步依賴「同系列其他集也處理過」，而那個前提不見得成立。

### 剩下的 0.925 → 0.95 缺口是什麼

全部是**實景鏡頭**被判成投影片：講堂全景、攝影棚定鏡、講者在講台。
跨集比對抓不到它們，因為講者的姿勢每一幀都不一樣。

三個方向，都還沒量：

1. **換分類模型**。措辭敏感度這麼高，換模型可能比繼續改措辭有效。
2. **實景鏡頭有 CV 特徵**——攝影棚定鏡在 S1c 本來就自己聚成一群
   （zIglvjoU9vo 17 張聚成 1 群）。但 D26 已經量過並否決了
   「用 CV 剔除攝影棚定鏡」——那等於讓 CV 做分類，正是 D16 移除的東西。
   要重提必須解釋為什麼這次不同。
3. **接受 0.925 並改門檻**。**不可以**——§5.5 #7 明文禁止在測試失敗時
   調門檻，而且分類錯的代價很高（把講者鏡頭判成投影片，攝影棚佈景的
   書法就成了 §5.4 的合法來源）。

### 樣本的限制

- v4/v5 的判準是**看過保留集的失敗案例之後**想出來的，
  `2FjApOVIbUs` 對它們已不是純粹的保留集。播放清單還有 26 支未動用。
- 四支影片、80 張代表幀。
- 只測了一個分類模型。

---

## R22：溯源通過率 0.838——但一半的未通過不是內容問題

完整拆解見 `experiments/r27_provenance_breakdown/`。
四支影片、167 個 block、通過 140。**27 筆未通過拆開後：**

| 成因 | 筆數 |
|---|---|
| **歸屬錯**：內容溯得到同一段的**另一個**來源 | **15** |
| 兩個來源都對不上——真正要判內容的 | 10 |
| 來源投影片其實不是投影片（R21 的下游後果） | 2 |

歸屬錯與分類誤報都修好的話會是 **157/167 = 0.940**。
仍未達 0.95，但那時剩下的 10 筆才是誠實的問題。

差距不是邊緣值：標錯的那個來源 containment 0.00–0.09，
對的那個 0.51–0.59。已做進 `_diagnose_wrong_source`，
記在 `BlockVerdict.wrong_source`，**不自動改判為通過**——
§3.5 要求 chunk 自足，「投影片寫了 X」與「講者說了 X」在下游是兩件事。

那 10 筆裡有 4 筆是「具名實體出現在投影片、不在該段逐字稿」，
與歸屬錯是同一件事的另一面；還有 1 筆是抽取器誤報
（「兩大原因」的「兩」被當成數字）。**真正可能是內容問題的不到 5 筆。**

### 已做：歸屬（S4c prompt v7），15 → 9 筆

把 `provenance_kind` 的說明改成三句可操作的判準，並**把 `provenance_ref`
從 schema 拿掉**——一段只會拿到一張投影片的文字，合法來源只有一個，
逐字稿的區間就是這一段的起訖，兩者都由管線填。

| | v3 prompt | v7 prompt |
|---|---|---|
| 通過率 | 140/167 = 0.838 | **135/155 = 0.871** |
| 歸屬錯 | 15 | **9** |
| 投影片段落卻 0 block | 0 | 0 |

**block 總數從 167 掉到 155**（−7%），總字數未逐版對照，
所以「通過率上升」有一部分可能來自分母變小。要再跑幾支才說得準。

中間繞了兩個彎，都記在 decisions.md D30：
v6 那一版我寫了整整一段（含數字表格）解釋怎麼標，還寫「`provenance_ref`
隨便填」，結果 qwen2.5:14b 被帶偏，**投影片段落產出 0 個 content_block**，
167 個 block 掉到 21 個——而**每一項機械檢查都是綠的**，
溯源通過率甚至更好看，因為分母小了。已加防護（`_MAX_BARREN_RATIO`）。

### 還沒做的順序

1. ~~歸屬~~ 已做，剩 9 筆
2. 分類紅燈（R21）——這四支只有 2 筆，但代價高：講者鏡頭判成投影片後，
   攝影棚佈景的書法就成了 §5.4 的合法來源
3. 具名實體抽取器的數字誤報——一行 regex
4. 剩下的才是「內容品質 vs 檢查適用性」，而樣本只剩個位數，**要先擴樣本**

### 已修：`provenance.ref` 不再由模型提供

3 筆未通過的原因是模型把 `slide_015` 寫成 `015` → 查不到 → 來源為空 →
失敗訊息顯示「來源長度僅 0%」，看起來像內容太長，其實是查無此人。
改用管線的權威值覆蓋（一段只會拿到一張投影片的文字，合法來源只有一個），
正規化放在快取命中與新呼叫**都會走**的路徑上（D22 同一個坑）。

---

## R23：per-video 溯源閘門目前擋掉 100% 的影片，`chunks.jsonl` 是空的

票 03 把門檻語意拆乾淨之後，第一次看得到這件事的全貌。
完整量測見 `experiments/r29_threshold_semantics/`。

| 影片 | 通過 | 閘門 | 歸屬錯 | 真的溯不到 |
|---|---|---|---|---|
| zIglvjoU9vo | 0.828 | **擋** | 5 | 6 |
| C_CFyilE-ks | 0.892 | **擋** | 3 | 1 |
| cxrqHABhWOU | 0.929 | **擋** | 1 | 0 |
| 2FjApOVIbUs | 0.900 | **擋** | 0 | 4 |

155 個 block 裡**只有 11 個（7.1%）真的溯不到來源**，9 個是歸屬標錯。

**而 §7.4 的收尾定義是「產出真的被使用」。** 現在管線跑得完、複核文件
產得出來，但知識庫拿不到任何東西。

### 這個閘門是疊在第二層的

`build_chunks` 已經逐 block 排除未通過的，所以**進到 `chunks.jsonl` 的
每個 chunk 本來就都通過溯源**。per-video 閘門的語意是「這支影片整體
不可信，連通過的部分也不要」。cxrqHABhWOU 是最極端的例子：
14 個 block 通過 13 個，唯一未通過的那個還是歸屬標錯，整支被丟掉。

### 沒有動它的理由

FROZEN F2：那個 5% 沒有依據，恢復觸發條件是**票 12 的可用性實測**，
不是回頭憑感覺換一個數字。

### 三個方向（待使用者決定）

1. 等票 12 用可用性實測訂閘門。最正確，收尾要多繞一圈。
2. 閘門改用「**真的溯不到**」的比例而非全部未通過。實測 7.1%，
   cxrqHABhWOU 會變成 0% 而通過。理由不是為了變綠——歸屬標錯的 block
   已經被逐 block 排除了，拿它否決整支是**重複計算同一個問題**。
3. 接受現狀，等票 08／10 降低未通過數之後再看。

---

## R24：產出裡混著簡體字與英文（**10.1%**，不是原本寫的 27.5%）

票 12 讀 chunk 時量到的。**第一版的數字是錯的，這裡是更正後的。**

| | |
|---|---|
| 真正只出現簡體寫法的 chunk | **7/69 = 10.1%** |
| 含英文單字（≥3 字母）的 chunk | 6/69 = 8.7% |

例子：`小 intestine`、`幽门杆菌`、`恶搞`、`妈妈`、`小肠`、`仓库`。

### 第一版為什麼算成 27.5%

我用 `s2tw` 逐字比對，把**任何被改寫的字**都算成簡體。但它會標記
**異體字**：`吃`（×10）、`回`（×10）、`了`（×4）、`游`（×4）、
`制`、`秘`、`群`——這些字**手動字幕裡也有**，也就是說它們是原文就有的
繁體／異體寫法，不是模型生成的簡體。

扣掉之後才是 7/69。

### ⚠️ 不要照字面去跑 s2t 或 s2tw

**`游` 是講者的姓**（游朱義，出現在 `2FjApOVIbUs#013`）。
對 S4c 的產出跑 `s2t` 會把它改成 `遊朱義`，還會把 `吃`→`喫`、`台`→`臺`。
那**正好製造這一條說要避免的 Obsidian 連結錯亂**，而且是在本來就正確的
文字上。D24 已經為同一件事踩過坑（`s2twp` 把 `局部`→`區域性`）。

要修的話只能針對**確認是簡體的那些字**做白名單式替換，
或在 prompt 上要求——而 D31 已經量過 14B 模型對長 prompt 的反應。

**成因仍然成立**：D24 只把 ASR 輸出轉繁，S4c 生成的內容沒有任何字集
正規化。這四支用手動字幕（本來就是繁體），所以那 7 個 chunk 的簡體字
**全部來自模型生成**。`小 intestine` 更明顯是翻譯殘留。

**對使用者的影響**：知識庫是繁體的，`小肠` 與 `小腸` 在 Obsidian 裡
是兩個不同的連結。10.1% 比 27.5% 輕，但不是零。


## R25：可用性指標有雛形了，但樣本太小不能訂門檻

FROZEN F2 說 §5.2 那個 0.95 的恢復觸發條件是「當有了產出好不好用的評測」。

| 候選 | 結論 |
|---|---|
| RAGAS | **不採用**——三個指標全要 LLM 當裁判，而那是「用本地 LLM 評判本地 LLM 的產出」，正是票 01 修掉的循環 |
| 檢索指標（R@k／MRR） | **有區辨力**，但樣本太小 |

同一組題（16 題）上：

| | R@1 | MRR |
|---|---|---|
| weft | **1.000** | **1.000** |
| naive 固定 200 字視窗 | 0.812 | 0.875 |
| 差距 | **+0.188** | +0.125 |

### 第一版的結論是錯的，記在這裡

我第一版寫的是「這個指標沒有區辨力（只差 0.055）」，並據此說 F2 的
觸發條件仍未滿足。**那個 0.055 是對照組寫錯造成的**：gold 對映用
「有 >0 秒重疊就算」，讓對照組在更容易的題目上計分
（58 個 naive chunk 拿到 gold 資格，21% 的重疊不到自身時長的四分之一）。

修對之後差距是 +0.188——**3.4 倍**。

> **R23 的教訓第二次發生**：對照組出錯時會偽裝成「訊號不準」。

### 為什麼仍然不訂門檻

1. **16 題太少**，而且排除規則恰好排掉了 weft 唯一失敗的那一題
   （「養肝有什麼方法？」rank 3）——weft 的 1.000 有一部分是運氣。
   全 18 題時是 0.944／0.963。
2. **語料只有 69 個 chunk、兩支影片。** 票 11 的 26 支 ≈ 800 個 chunk，
   干擾項多 10 倍之後差距會放大還是縮小沒有資料。
3. 票 12 自己寫的紀律：「不憑感覺訂門檻——首版只記錄」。

**與第一版的差別**：那時的結論是「這條路走不通」，
現在是「**這條路走得通，但要更多資料**」。票 11 跑完之後重量。


---

## R26：S4b 的詞庫假設投影片與語音同語言

2026-08-09，R36。機器人學課程（中文授課、**英文投影片**）：

```
slide_text: "NCU - Human and Machine lab  Chapter 2 – Spatial Representation…"
詞庫： 0 詞 ／ 2 詞 ／ 0 詞     校正： 三支都是 0 筆
```

S4b 從 `slide_text` 建詞庫（D25），隱含假設兩者同語言。
**中文授課配英文投影片在台灣的大學課程是常態**，這個組合下
整個 S4b 產不出任何東西。

**不是壞掉，是假設不成立。** 要改得先決定「英文術語該不該用來校正
中文 ASR」——那不是一個實作問題。


---

## R27：S4c 會默默修好 ASR 的錯字，而那不算在任何一種成因裡

2026-08-09，R36。`UiKi5-Arce4#014`，相似度 0.039：

```
block 寫的：  「坐標系B中的點P」「4x4 的矩陣」
逐字稿 raw：  「這個做法系統…有一個位置叫PB」
```

Whisper 把「**座標**」聽成「**做標**」（等長同音，R18 描述的典型形態），
而 S4c 在寫 block 時**自己改對了**。溯源對 `transcript_raw` 比對因此失敗
——**判定是對的**，那些字確實不在原始逐字稿裡。

但成因被歸到「兩個來源都對不上」，而那一欄正是拿來判斷
「內容有沒有問題」的——**混進去會高估內容問題**。

`depends_on_correction`（票 01）抓不到它：那個機制假設校正都經過 S4b
並留下 `Correction` 紀錄，而這裡的校正**發生在模型腦裡**。

另一半是**數學符號**：模型把口說的「四乘四」寫成 `4x4`、
「R 一一」寫成 `R₁₁`。字元 n-gram 對這種正規化無能為力。
STEM 素材上這是常態不是偶發。

**修法方向**（都沒做）：把 R13 的拼音比對接到溯源診斷上，
分出「同音替換」與「真的沒有出處」。但要先量它會不會製造誤報——
D25 的教訓是在錯誤基率高的資料上量出的 precision 會虛高。


---

## R28：`depends_on_correction` 至今零樣本，三個不同的原因

票 01 做完到現在，這個機制**沒有任何真實樣本**：

| 素材 | Whisper？ | 有投影片？ | 為什麼測不到 |
|---|---|---|---|
| 中醫講經 ×4 | ✗ | ✓ | 手動字幕，校正極少 |
| 大问题 | ✓ | ✗ | 沒有投影片 → 沒有詞庫 |
| 機器人學 | ✓ | ✓ | **投影片是英文 → 詞庫空的**（R26）|

R28 的實驗記錄當時預測「機器人學才是第一次」。**那個預測錯了。**

要測到它，需要**中文投影片 + 無字幕**的素材。目前手上沒有。


---

## R29：S1c 的獨立交叉檢查，在投影片型 STEM 素材上兩個方向同時失效

`tests/test_unit_dedup.py::test_grouping_agrees_with_an_independent_metric`
的前提是：**S1c 合併的那些，像素差應該小；沒合併的，像素差應該大。**
R25 在中醫講經上用 1,000+ 對驗過這條，一致。

加進 `UiKi5-Arce4`（NYCU 機器人學）之後它變紅：
「未合併最小 0.0149 ≤ 已合併最大 0.5163」。**逐張開圖看過，S1c 是對的。**

| | 實際是什麼 | S1c | 像素 MAE | 誰錯 |
|---|---|---|---|---|
| slide_082/093/103/110 | **講者在黑板前的鏡頭**，同場景不同姿勢 | 合併 ✓ | 0.42–0.52（判為誤併）| 像素 |
| slide_005 vs slide_008 | **兩張不同的投影片**（Position Vector / Orientation Matrix），共用模板 | 不併 ✓ | 0.0149（判為漏併）| 像素 |

**兩個假設同時反轉**，原因相反：講者會動所以同場景像素差大；
投影片共用標題列與校徽浮水印所以不同內容像素差小。
中醫講經沒有這兩種形狀（背板靜止、投影片滿版無模板）。

**處置**：測試範圍改為「`slide_groups` 非空」——也就是**人工複核過分組的
影片**。原本的範圍是「有沒有黃金集檔案」，而票 06 之後那兩個集合分家了
（有些黃金集只標內容邊界）。既有四支的分組驗證照跑，覆蓋沒縮水。

**沒有**放寬 assertion 讓它變綠（§5.5 #7）。

> 一般化的教訓：**兩個獨立度量畫不出同一條線時，也可能是它們量的東西
> 不同**，不必然是演算法錯。要判斷是哪一種，只能去看素材本身。

---

## R30：`BOUNDARY_TOLERANCE_SEC = 20` 在現行刀數密度下量不出東西

R37 實測，等距切同樣多刀時容忍窗涵蓋的時間比例：

| 影片 | 本文 | 刀數 | ±5s | ±10s | **±20s** | ±30s |
|---|---|---|---|---|---|---|
| `cxrqHABhWOU` | 14 分 | 18 | 21% | 42% | **83%** | 100% |
| `2FjApOVIbUs` | 25 分 | 30 | 20% | 40% | **80%** | 100% |
| `UiKi5-Arce4` | 40 分 | 57 | 24% | 48% | **95%** | 100% |

**±30s 在三支上都是 100%**：任何方法都會拿高分。±20s 已在同一個區域。

後果有兩層：

1. **R30 那場對打（票 07）的結論建立在這個容忍窗上。** 它選出了現在
   正在跑的 `ngram`，理由寫的是「與 Sentence-BERT 打平（都是 0.667）」。
   ±10s 下**不是打平**：講經保留集 ngram 0.400 vs MiniLM 0.476。
   `block_chars=40` 那次掃描同樣是在 ±20s 下做的——**等於沒有量過**。
2. **它會製造反向的假結論。** STEM 保留集在 ±20s 下所有方案都「輸給
   等距切」（−0.03），讀起來像「語意分段在 STEM 上沒用」；
   ±10s 下 ngram 對等距 **+0.30**，是三支裡優勢最大的。

**沒有改 `BOUNDARY_TOLERANCE_SEC`。** ±10s 是**看了覆蓋率之後選的**，
不是事先訂的；「多少才對」沒有獨立依據，而依覆蓋率反推會讓門檻
隨刀數浮動。要訂得先決定「分段邊界差幾秒算對」這個產品問題——
那不是一個實作問題。`tests/test_e2e_pipeline.py` 目前仍用 20。

配套：任何用 `boundary_prf` 報數字的地方，**要一起報等距對照組**
（R23 的教訓：對照組缺席時，密度給的分數會被讀成方法的效果）。

---

## R31：量測腳本讀生產目錄，會讀到後來的實作改了什麼

R37 重跑 R30 的對打時，`slide`（v0.4 投影片切換基線）那一列與 `ngram`
**逐位元相同**，刀數也相同（18／30／57，R30 當時是 9／26）。

原因：`bakeoff.slide_boundaries()` 讀 `work/<vid>/06_segments.json`，
而**票 08 之後那個檔裡裝的就是 ngram 的結果**。

差一點把新方法冠上舊方法的名字報出去，並得到「現行投影片方案其實不差」
這種完全反的結論。腳本現在會偵測 `slide == ngram` 並丟掉那一列；
**舊基線已不可得**，除非把 pipeline 倒回票 08 之前重跑。

> **這是第二次。** 上一次是量測腳本呼叫 `s3_align`，**寫進了**生產資料。
> 一次是讀、一次是寫，同一個根源：**量測與生產共用 `work/`**。
